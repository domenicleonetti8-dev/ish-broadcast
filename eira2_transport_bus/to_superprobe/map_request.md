EIRA mapping request

Read-only request for the local Superprobe/Watcher side.

Please return an evidence-backed canonical map of EIRA LIVE through from_superprobe. Include the canonical orchestration order, file paths and byte sizes, hashes, semantic dependencies, runtime relationships, bridges, nodes, extensions, registries, neural atlas fibers, and the existing AR/hologram topology metadata. Correlate AR topology with canonical neural nodes where evidence supports it.

Build only a non-executing isolated reconstruction model. Do not modify LIVE. Label relationships as verified, observed, static, inferred, unknown, or unresolved rather than inventing missing links.

Requested result families: brain map, orchestration map, byte ledger, semantic graph, runtime graph, bridge map, node map, neural/AR fusion map, reconstruction manifest, and forensic receipt.
