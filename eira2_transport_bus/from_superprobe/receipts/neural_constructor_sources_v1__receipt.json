{
  "builder_invoked": false,
  "completed_unix": 1788830761.843165,
  "error": null,
  "evidence": {
    "candidate_files_scanned": 133,
    "evidence_sha256": "df19689445b3d8767ce735a53aa5e62a2965d2728397e9890d523169f43735cf",
    "full_sources": [
      {
        "bytes": 7111,
        "path": "eira2/neural/organism.py",
        "sha256": "fbe4ab42e1a2ecc4bd45a01da548d340312dcdf29f54124ef659d424a0392b01",
        "source": "from __future__ import annotations\n\nfrom pathlib import Path\nfrom typing import Any\n\nfrom ..contracts import Event, RuntimeContext, ServiceSpec\nfrom ..kernel.events import EventService\nfrom ..registry.graph import NodeRegistryService\nfrom .atlas import NeuralAtlas, stable_neuron_id\nfrom .communication import CommunicationLiveNeuralHTTPService, PCMSubmitter, TextSubmitter\nfrom .live import LiveNeuralSurface\n\n\nclass RegistryAwareNeuralSurface(LiveNeuralSurface):\n    \"\"\"Live atlas that overlays canonical runtime nodes and proven bridges.\"\"\"\n\n    def __init__(\n        self,\n        root: Path,\n        *,\n        inventory: Path | None = None,\n        trace: bool = True,\n        sync_interval_seconds: float = 2.0,\n    ) -> None:\n        super().__init__(\n            root,\n            inventory=inventory,\n            trace=trace,\n            sync_interval_seconds=sync_interval_seconds,\n        )\n        self._runtime_bridges: dict[tuple[str, str, str], dict[str, Any]] = {}\n\n    def _inject_runtime_nodes(self, atlas: NeuralAtlas) -> None:\n        super()._inject_runtime_nodes(atlas)\n        for row in self._runtime_bridges.values():\n            source_id = stable_neuron_id(f\"@runtime/{row['source']}\")\n            target_id = stable_neuron_id(f\"@runtime/{row['target']}\")\n            if source_id not in atlas.neurons or target_id not in atlas.neurons:\n                continue\n            atlas._add_fiber(\n                source_id,\n                target_id,\n                str(row.get(\"kind\") or \"runtime_call\"),\n                bool(row.get(\"conductive\", True)),\n                str(row.get(\"evidence\") or \"runtime_registry_bridge\"),\n            )\n\n    def seed_registry(self, snapshot: dict[str, Any]) -> dict[str, Any]:\n        for node in snapshot.get(\"nodes\", []):\n            if not isinstance(node, dict):\n                continue\n            name = str(node.get(\"name\") or \"\").strip()\n            if not name:\n                continue\n            self._runtime_nodes[name] = {\n                \"kind\": str(node.get(\"kind\") or \"service\")[:80],\n                \"cluster\": f\"eira2:{name.split('.', 1)[0]}\",\n            }\n        for bridge in snapshot.get(\"bridges\", []):\n            if not isinstance(bridge, dict):\n                continue\n            source = str(bridge.get(\"source\") or \"\").strip()\n            target = str(bridge.get(\"target\") or \"\").strip()\n            kind = str(bridge.get(\"kind\") or \"runtime_call\")[:80]\n            if not source or not target:\n                continue\n            self._runtime_bridges[(source, target, kind)] = {\n                \"source\": source,\n                \"target\": target,\n                \"kind\": kind,\n                \"conductive\": bool(bridge.get(\"conductive\", True)),\n                \"evidence\": str(bridge.get(\"evidence\") or \"registry_snapshot\")[:160],\n            }\n        return self.refresh_topology(\"registry_seed\")\n\n    def register_runtime_bridge(\n        self,\n        source: str,\n        target: str,\n        *,\n        kind: str = \"runtime_call\",\n        conductive: bool = True,\n        evidence: str = \"registry_event\",\n    ) -> dict[str, Any]:\n        source = str(source or \"\").strip()\n        target = str(target or \"\").strip()\n        kind = str(kind or \"runtime_call\")[:80]\n        if not source or not target:\n            return {\"changed\": False, \"error\": \"runtime_bridge_identity_required\"}\n        self._runtime_bridges[(source, target, kind)] = {\n            \"source\": source,\n            \"target\": target,\n            \"kind\": kind,\n            \"conductive\": bool(conductive),\n            \"evidence\": str(evidence or \"registry_event\")[:160],\n        }\n        return self.refresh_topology(f\"runtime_bridge_registered:{source}:{target}:{kind}\")\n\n\nclass OrganismNeuralHTTPService(CommunicationLiveNeuralHTTPService):\n    \"\"\"Canonical iPhone communication plus the live EIRA2 neural organism.\"\"\"\n\n    spec = ServiceSpec(\n        \"interfaces.neural_ui\",\n        required=False,\n        dependencies=(\n            \"kernel.events\",\n            \"registry.graph\",\n            \"interfaces.ingress\",\n            \"interfaces.native\",\n            \"delivery.reactive_presence\",\n            \"conversation.spine\",\n            \"delivery.gate\",\n        ),\n        start_timeout_seconds=60.0,\n        stop_timeout_seconds=10.0,\n    )\n\n    def __init__(\n        self,\n        root: Path,\n        *,\n        text_submitter: TextSubmitter | None = None,\n        pcm_submitter: PCMSubmitter | None = None,\n        host: str = \"127.0.0.1\",\n        port: int = 8782,\n        inventory: Path | None = None,\n        trace: bool = True,\n        sync_interval_seconds: float = 2.0,\n    ) -> None:\n        super().__init__(\n            root,\n            host=host,\n            port=port,\n            inventory=inventory,\n            trace=trace,\n            text_submitter=text_submitter,\n            pcm_submitter=pcm_submitter,\n        )\n        self.surface = RegistryAwareNeuralSurface(\n            root,\n            inventory=inventory,\n            trace=trace,\n            sync_interval_seconds=sync_interval_seconds,\n        )\n        self._bridge_subscription_installed = False\n\n    async def _bridge_event(self, event: Event) -> None:\n        payload = dict(event.payload or {})\n        self.surface.register_runtime_bridge(\n            str(payload.get(\"source\") or \"\"),\n            str(payload.get(\"target\") or \"\"),\n            kind=str(payload.get(\"kind\") or \"runtime_call\"),\n            conductive=bool(payload.get(\"conductive\", True)),\n            evidence=str(payload.get(\"evidence\") or event.topic),\n        )\n\n    async def start(self, context: RuntimeContext) -> None:\n        try:\n            registry = context.get_service(\"registry.graph\")\n            if isinstance(registry, NodeRegistryService):\n                self.surface.seed_registry(registry.snapshot())\n        except Exception:\n            # Optional visualization must not become a lifecycle authority.\n            pass\n        if not self._bridge_subscription_installed:\n            events = context.get_service(\"kernel.events\")\n            if isinstance(events, EventService):\n                events.subscribe(\"registry.bridge_registered\", self._bridge_event)\n                self._bridge_subscription_installed = True\n        await super().start(context)\n\n\ndef register() -> dict[str, object]:\n    return {\n        \"name\": \"eira2.neural.organism\",\n        \"kind\": \"registry_aware_neural_organism_and_communication_surface\",\n        \"status\": \"online\",\n        \"isolated\": True,\n        \"file_neurons\": True,\n        \"runtime_nodes\": True,\n        \"runtime_bridges\": True,\n        \"automatic_topology_sync\": True,\n        \"real_execution_activity_only\": True,\n        \"iphone_text_endpoint\": \"/v1/text\",\n        \"iphone_pcm_endpoint\": \"/v1/listen\",\n        \"iphone_ambient_endpoint\": \"/v1/ambient\",\n        \"ambient_wake_gate\": True,\n        \"single_conversation_authority\": \"conversation.spine\",\n        \"single_delivery_authority\": \"delivery.gate\",\n        \"invention_usdz_surface\": \"/inventions\",\n        \"synthetic_membership_conductive\": False,\n    }\n"
      },
      {
        "bytes": 12344,
        "path": "eira2/neural/live.py",
        "sha256": "2e54b6a05e5ab231e7b9d69f7f304e25e4a12a72f8e1c02a4b957438d2bc6bb5",
        "source": "from __future__ import annotations\n\nimport threading\nimport time\nfrom collections import deque\nfrom pathlib import Path\nfrom typing import Any\n\nfrom ..contracts import Event, RuntimeContext\nfrom ..kernel.events import EventService\nfrom .apple_ar import build_brain_usdz\nfrom .atlas import NeuralAtlas, Neuron, _anatomy_for, stable_neuron_id\nfrom .growth import overlay_live_filesystem\nfrom .server import NeuralHTTPService, NeuralSurface\nfrom .sync import NeuralTopologySynchronizer\n\n\nclass AtomicAtlasProxy:\n    \"\"\"Stable object identity with atomically replaceable immutable atlas builds.\"\"\"\n\n    def __init__(self, atlas: NeuralAtlas) -> None:\n        self._atlas = atlas\n        self._lock = threading.RLock()\n        self.generation = 1\n        self.last_change: dict[str, Any] = {\n            \"generation\": 1,\n            \"reason\": \"initial_build\",\n            \"added\": 0,\n            \"removed\": 0,\n            \"changed_at_unix\": time.time(),\n        }\n\n    def target(self) -> NeuralAtlas:\n        with self._lock:\n            return self._atlas\n\n    def swap(self, atlas: NeuralAtlas, *, reason: str, added: int, removed: int) -> int:\n        with self._lock:\n            self._atlas = atlas\n            self.generation += 1\n            self.last_change = {\n                \"generation\": self.generation,\n                \"reason\": str(reason or \"topology_change\")[:160],\n                \"added\": max(0, int(added)),\n                \"removed\": max(0, int(removed)),\n                \"changed_at_unix\": time.time(),\n            }\n            return self.generation\n\n    def overview(self) -> dict[str, Any]:\n        row = self.target().overview()\n        row[\"topology_generation\"] = self.generation\n        row[\"last_topology_change\"] = dict(self.last_change)\n        row[\"automatic_topology_sync\"] = True\n        row[\"canonical_baseline_entries\"] = 76056\n        row[\"growth_above_baseline\"] = max(0, int(row.get(\"inventory_neuron_count\") or 0) - 76056)\n        return row\n\n    def __getattr__(self, name: str) -> Any:\n        return getattr(self.target(), name)\n\n\nclass LiveNeuralSurface(NeuralSurface):\n    \"\"\"Neural surface whose topology follows EIRA 2.0 structural changes.\"\"\"\n\n    def __init__(\n        self,\n        root: Path,\n        *,\n        inventory: Path | None = None,\n        trace: bool = True,\n        sync_interval_seconds: float = 2.0,\n    ) -> None:\n        root = root.expanduser().resolve()\n        baseline = NeuralAtlas(root, inventory=inventory).build()\n        detected_inventory: Path | None = None\n        if inventory is not None:\n            detected_inventory = inventory.expanduser().resolve()\n        elif baseline.inventory_source and baseline.inventory_source != \"filesystem_scan_fallback\":\n            candidate = Path(baseline.inventory_source)\n            if candidate.is_file():\n                detected_inventory = candidate.resolve()\n\n        self.root = root\n        self._inventory = detected_inventory\n        initial = NeuralAtlas(root, inventory=self._inventory).build()\n        overlay_live_filesystem(initial, root)\n        self._runtime_nodes: dict[str, dict[str, str]] = {}\n        self._refresh_lock = threading.RLock()\n        self._revisions: deque[dict[str, Any]] = deque(maxlen=128)\n        self.atlas = AtomicAtlasProxy(initial)\n        from .activity import NeuralActivity\n\n        self.activity = NeuralActivity(root, atlas=self.atlas)\n        self.trace = bool(trace)\n        self.invention_usdz_root = self.root / \"var\" / \"inventions\" / \"usdz\"\n        self.apple_ar_brain = self.invention_usdz_root / \"eira2_brain.usdz\"\n        self.sync = NeuralTopologySynchronizer(\n            root,\n            inventory=self._inventory,\n            on_change=self.refresh_topology,\n            interval_seconds=sync_interval_seconds,\n        )\n\n    @staticmethod\n    def _ensure_cluster(atlas: NeuralAtlas, cluster: str) -> str:\n        cluster = str(cluster or \"eira2:runtime\")\n        logical = f\"@cluster/{cluster}\"\n        existing = atlas.by_logical.get(logical)\n        if existing:\n            return existing\n        atlas.clusters.setdefault(cluster, [])\n        nid = stable_neuron_id(logical)\n        hemisphere, spine = _anatomy_for(cluster)\n        atlas.neurons[nid] = Neuron(\n            id=nid,\n            path=logical,\n            logical_path=logical,\n            kind=\"cluster\",\n            cluster=cluster,\n            hemisphere=hemisphere,\n            spine=spine,\n            disposition=\"synthetic_anatomical_cluster\",\n            activity_class=\"cluster\",\n            size=0,\n            suffix=\"\",\n            conductive_capable=False,\n            synthetic=True,\n        )\n        atlas.by_logical[logical] = nid\n        return nid\n\n    def _inject_runtime_nodes(self, atlas: NeuralAtlas) -> None:\n        for name, spec in sorted(self._runtime_nodes.items()):\n            cluster = spec.get(\"cluster\") or \"eira2:runtime\"\n            cluster_id = self._ensure_cluster(atlas, cluster)\n            logical = f\"@runtime/{name}\"\n            nid = stable_neuron_id(logical)\n            hemisphere, spine = _anatomy_for(cluster)\n            atlas.neurons[nid] = Neuron(\n                id=nid,\n                path=logical,\n                logical_path=logical,\n                kind=spec.get(\"kind\") or \"runtime_node\",\n                cluster=cluster,\n                hemisphere=hemisphere,\n                spine=spine,\n                disposition=\"runtime_registered_node\",\n                activity_class=\"runtime\",\n                size=0,\n                suffix=\"\",\n                conductive_capable=False,\n                synthetic=True,\n            )\n            atlas.by_logical[logical] = nid\n            if nid not in atlas.clusters[cluster]:\n                atlas.clusters[cluster].append(nid)\n            atlas._add_fiber(\n                cluster_id,\n                nid,\n                \"anatomical_membership\",\n                False,\n                \"runtime_node_registration\",\n            )\n\n    def _new_atlas(self) -> NeuralAtlas:\n        atlas = NeuralAtlas(self.root, inventory=self._inventory).build()\n        overlay_live_filesystem(atlas, self.root)\n        self._inject_runtime_nodes(atlas)\n        return atlas\n\n    def _rebuild_apple_ar(self) -> None:\n        build_brain_usdz(self.atlas, self.apple_ar_brain)\n\n    def refresh_topology(self, reason: str = \"topology_change\") -> dict[str, Any]:\n        with self._refresh_lock:\n            previous = self.atlas.target()\n            next_atlas = self._new_atlas()\n            previous_ids = set(previous.neurons)\n            next_ids = set(next_atlas.neurons)\n            previous_fibers = {(f.source, f.target, f.kind, f.evidence) for f in previous.fibers}\n            next_fibers = {(f.source, f.target, f.kind, f.evidence) for f in next_atlas.fibers}\n            added = len(next_ids - previous_ids)\n            removed = len(previous_ids - next_ids)\n            topology_changed = bool(added or removed or previous_fibers != next_fibers)\n            if not topology_changed:\n                return {\n                    \"changed\": False,\n                    \"generation\": self.atlas.generation,\n                    \"reason\": reason,\n                    \"added\": 0,\n                    \"removed\": 0,\n                }\n            generation = self.atlas.swap(\n                next_atlas,\n                reason=reason,\n                added=added,\n                removed=removed,\n            )\n            self._rebuild_apple_ar()\n            revision = {\n                \"generation\": generation,\n                \"reason\": str(reason or \"topology_change\")[:160],\n                \"added\": added,\n                \"removed\": removed,\n                \"at_unix\": time.time(),\n            }\n            self._revisions.append(revision)\n            return {\"changed\": True, **revision}\n\n    def register_runtime_node(\n        self,\n        name: str,\n        *,\n        kind: str = \"runtime_node\",\n        cluster: str = \"eira2:runtime\",\n    ) -> dict[str, Any]:\n        clean = \" \".join(str(name or \"\").split())[:200]\n        if not clean:\n            return {\"changed\": False, \"error\": \"runtime_node_name_required\"}\n        self._runtime_nodes[clean] = {\n            \"kind\": str(kind or \"runtime_node\")[:80],\n            \"cluster\": str(cluster or \"eira2:runtime\")[:160],\n        }\n        return self.refresh_topology(f\"runtime_node_registered:{clean}\")\n\n    def unregister_runtime_node(self, name: str) -> dict[str, Any]:\n        clean = \" \".join(str(name or \"\").split())[:200]\n        if clean not in self._runtime_nodes:\n            return {\"changed\": False, \"generation\": self.atlas.generation}\n        self._runtime_nodes.pop(clean, None)\n        return self.refresh_topology(f\"runtime_node_unregistered:{clean}\")\n\n    def notify_file_or_extension_change(self, reason: str) -> None:\n        self.sync.notify(reason)\n\n    def revisions_since(self, generation: int) -> list[dict[str, Any]]:\n        return [row.copy() for row in self._revisions if int(row[\"generation\"]) > int(generation)]\n\n    def start(self) -> None:\n        self.invention_usdz_root.mkdir(parents=True, exist_ok=True)\n        self._rebuild_apple_ar()\n        if self.trace:\n            self.activity.start_trace()\n        self.sync.start()\n\n    def stop(self) -> None:\n        self.sync.stop()\n        self.activity.stop_trace()\n\n\nclass LiveNeuralHTTPService(NeuralHTTPService):\n    \"\"\"Drop-in HTTP service with automatic in-place neural topology updates.\"\"\"\n\n    def __init__(\n        self,\n        root: Path,\n        *,\n        host: str = \"127.0.0.1\",\n        port: int = 8782,\n        inventory: Path | None = None,\n        trace: bool = True,\n    ) -> None:\n        super().__init__(root, host=host, port=port, inventory=inventory, trace=trace)\n        self.surface = LiveNeuralSurface(root, inventory=inventory, trace=trace)\n        self._subscriptions_installed = False\n\n    async def _topology_event(self, event: Event) -> None:\n        payload = dict(event.payload or {})\n        if event.topic in {\"runtime.node_registered\", \"registry.node_registered\"}:\n            self.surface.register_runtime_node(\n                str(payload.get(\"name\") or payload.get(\"node\") or payload.get(\"id\") or \"\"),\n                kind=str(payload.get(\"kind\") or \"runtime_node\"),\n                cluster=str(payload.get(\"cluster\") or \"eira2:runtime\"),\n            )\n            return\n        if event.topic in {\"runtime.node_unregistered\", \"registry.node_unregistered\"}:\n            self.surface.unregister_runtime_node(\n                str(payload.get(\"name\") or payload.get(\"node\") or payload.get(\"id\") or \"\")\n            )\n            return\n        self.surface.notify_file_or_extension_change(event.topic)\n\n    async def start(self, context: RuntimeContext) -> None:\n        if not self._subscriptions_installed:\n            events = context.get_service(\"kernel.events\")\n            if isinstance(events, EventService):\n                for topic in (\n                    \"runtime.node_registered\",\n                    \"runtime.node_unregistered\",\n                    \"registry.node_registered\",\n                    \"registry.node_unregistered\",\n                    \"runtime.extension_registered\",\n                    \"runtime.extension_unregistered\",\n                    \"registry.extension_registered\",\n                    \"registry.extension_unregistered\",\n                    \"runtime.file_added\",\n                    \"runtime.file_removed\",\n                    \"runtime.file_changed\",\n                ):\n                    events.subscribe(topic, self._topology_event)\n                self._subscriptions_installed = True\n        await super().start(context)\n\n\ndef register() -> dict[str, object]:\n    return {\n        \"name\": \"eira2.neural.live_surface\",\n        \"kind\": \"automatically_synchronized_neural_surface\",\n        \"status\": \"online\",\n        \"isolated\": True,\n        \"automatic_topology_sync\": True,\n        \"canonical_baseline_preserved\": True,\n        \"growth_above_baseline_supported\": True,\n        \"runtime_node_events_supported\": True,\n        \"file_add_remove_change_supported\": True,\n        \"extension_add_remove_supported\": True,\n        \"apple_ar_brain_from_same_atlas\": True,\n        \"apple_ar_href\": \"/inventions/usdz/eira2_brain.usdz\",\n        \"synthetic_membership_conductive\": False,\n    }\n"
      },
      {
        "bytes": 20492,
        "path": "eira2/neural/atlas.py",
        "sha256": "345d40ffd25939e4463f8a03b61e9dfea594417967f51d6bf5ec051183a718ed",
        "source": "from __future__ import annotations\n\nimport ast\nimport csv\nimport hashlib\nimport json\nimport os\nfrom collections import Counter, defaultdict\nfrom dataclasses import dataclass\nfrom pathlib import Path, PurePosixPath\nfrom typing import Any, Iterable, Iterator\n\n\n_INVENTORY_ENV = \"EIRA2_NEURAL_INVENTORY\"\n_EXCLUDED_SCAN_PARTS = {\".git\", \"__pycache__\", \".pytest_cache\"}\n_CODE_SUFFIXES = {\".py\", \".js\", \".ts\", \".tsx\", \".jsx\", \".html\", \".css\", \".json\", \".yaml\", \".yml\", \".toml\", \".sh\"}\n_CONDUCTIVE_KINDS = {\"python_import\", \"declared_runtime_dependency\", \"runtime_call\"}\n\n\ndef _normalize(value: Any) -> str:\n    return str(value or \"\").replace(\"\\\\\", \"/\").strip()\n\n\ndef stable_neuron_id(path: str) -> str:\n    \"\"\"Stable opaque neuron id; paths remain metadata, not identifiers.\"\"\"\n    clean = _normalize(path)\n    digest = hashlib.blake2b(clean.encode(\"utf-8\", errors=\"replace\"), digest_size=12).hexdigest()\n    return f\"neuron:{digest}\"\n\n\ndef _logical_path(raw: str, root: Path) -> str:\n    value = _normalize(raw)\n    if not value:\n        return \"\"\n    root_text = _normalize(root.resolve())\n    if value == root_text:\n        return \".\"\n    if value.startswith(root_text.rstrip(\"/\") + \"/\"):\n        return value[len(root_text.rstrip(\"/\")) + 1 :]\n    marker = \"/EIRA/LIVE/\"\n    pos = value.casefold().find(marker.casefold())\n    if pos >= 0:\n        return value[pos + len(marker) :]\n    if value.startswith(\"pi:/\"):\n        return \"external/pi/\" + value[3:].lstrip(\"/\")\n    if value.startswith(\"/\"):\n        return \"external/fs/\" + value.lstrip(\"/\")\n    return value.lstrip(\"./\")\n\n\ndef _entry_path(row: dict[str, Any]) -> str:\n    for key in (\"source_path\", \"path\", \"absolute_path\", \"relative_path\"):\n        if row.get(key):\n            return _normalize(row[key])\n    return \"\"\n\n\ndef _entry_kind(row: dict[str, Any]) -> str:\n    raw = str(row.get(\"entry_type\") or row.get(\"type\") or row.get(\"kind\") or \"file\").casefold()\n    return \"directory\" if raw in {\"dir\", \"directory\", \"folder\"} else \"file\"\n\n\ndef _disposition(row: dict[str, Any]) -> str:\n    return str(row.get(\"disposition\") or row.get(\"classification\") or \"unclassified\")\n\n\ndef _iter_json_payload(value: Any) -> Iterator[dict[str, Any]]:\n    if isinstance(value, list):\n        for row in value:\n            if isinstance(row, dict):\n                yield row\n        return\n    if isinstance(value, dict):\n        for key in (\"entries\", \"files\", \"manifest\", \"inventory\"):\n            rows = value.get(key)\n            if isinstance(rows, list):\n                for row in rows:\n                    if isinstance(row, dict):\n                        yield row\n                return\n        if _entry_path(value):\n            yield value\n            return\n    raise ValueError(\"neural_inventory_json_shape_unsupported\")\n\n\ndef inventory_rows(path: Path) -> Iterator[dict[str, Any]]:\n    suffix = path.suffix.casefold()\n    if suffix == \".csv\":\n        with path.open(\"r\", encoding=\"utf-8\", errors=\"replace\", newline=\"\") as handle:\n            yield from csv.DictReader(handle)\n        return\n    text = path.read_text(encoding=\"utf-8\", errors=\"replace\")\n    if not text.strip():\n        return\n    try:\n        payload = json.loads(text)\n    except json.JSONDecodeError:\n        for number, line in enumerate(text.splitlines(), 1):\n            if not line.strip():\n                continue\n            try:\n                row = json.loads(line)\n            except json.JSONDecodeError as error:\n                raise ValueError(f\"neural_inventory_jsonl_invalid:{number}:{error.msg}\") from error\n            if not isinstance(row, dict):\n                raise ValueError(f\"neural_inventory_row_not_object:{number}\")\n            yield row\n        return\n    yield from _iter_json_payload(payload)\n\n\ndef inventory_candidates(root: Path) -> tuple[Path, ...]:\n    env = os.getenv(_INVENTORY_ENV, \"\").strip()\n    values = []\n    if env:\n        values.append(Path(env).expanduser())\n    values.extend(\n        [\n            root / \"migration_ledger.csv\",\n            root / \"eira2\" / \"migration_ledger.csv\",\n            root / \"var\" / \"migration_ledger.csv\",\n            root / \"eira2\" / \"var\" / \"migration_ledger.csv\",\n            root / \"eira2_organization.json\",\n            root / \"eira2\" / \"eira2_organization.json\",\n            root / \"var\" / \"eira2_organization.json\",\n        ]\n    )\n    out: list[Path] = []\n    for value in values:\n        resolved = value.resolve() if value.is_absolute() else (root / value).resolve()\n        if resolved not in out:\n            out.append(resolved)\n    return tuple(out)\n\n\ndef _filesystem_rows(root: Path) -> Iterator[dict[str, Any]]:\n    for path in root.rglob(\"*\"):\n        try:\n            rel = path.relative_to(root)\n        except ValueError:\n            continue\n        if any(part in _EXCLUDED_SCAN_PARTS for part in rel.parts):\n            continue\n        try:\n            is_dir = path.is_dir()\n            size = 0 if is_dir else path.stat().st_size\n        except OSError:\n            continue\n        yield {\n            \"path\": str(path),\n            \"type\": \"directory\" if is_dir else \"file\",\n            \"size\": size,\n            \"disposition\": \"runtime_scan_fallback\",\n        }\n\n\ndef _cluster_for(logical: str) -> str:\n    parts = PurePosixPath(logical).parts\n    if len(parts) >= 2 and parts[0] == \"extensions\":\n        return f\"extension:{parts[1]}\"\n    if len(parts) >= 3 and parts[:2] == (\"eira2\", \"eira2\"):\n        return f\"eira2:{parts[2]}\"\n    if parts:\n        return f\"root:{parts[0]}\"\n    return \"root:.\"\n\n\ndef _anatomy_for(cluster: str) -> tuple[str, bool]:\n    low = cluster.casefold()\n    spine = any(token in low for token in (\"conversation\", \"kernel\", \"state\", \"memory\", \"delivery\", \"evidence\"))\n    if spine:\n        return \"midline\", True\n    bit = int(hashlib.blake2b(cluster.encode(), digest_size=1).hexdigest(), 16) & 1\n    return (\"left\" if bit == 0 else \"right\"), False\n\n\ndef _activity_class(disposition: str, logical: str) -> str:\n    low = disposition.casefold()\n    if low == \"migrate_runtime\" or logical.startswith((\"eira2/\", \"extensions/\")):\n        return \"runtime\"\n    if low == \"migrate_state\":\n        return \"state\"\n    if low == \"migrate_artifact\":\n        return \"artifact\"\n    if low in {\"retain_evidence\", \"regenerate\", \"external_dependency\", \"secret_reference\"}:\n        return low\n    return \"other\"\n\n\n@dataclass(frozen=True, slots=True)\nclass Neuron:\n    id: str\n    path: str\n    logical_path: str\n    kind: str\n    cluster: str\n    hemisphere: str\n    spine: bool\n    disposition: str\n    activity_class: str\n    size: int\n    suffix: str\n    conductive_capable: bool\n    synthetic: bool = False\n\n    def as_dict(self, *, include_path: bool = True) -> dict[str, Any]:\n        row = {\n            \"id\": self.id,\n            \"logical_path\": self.logical_path,\n            \"kind\": self.kind,\n            \"cluster\": self.cluster,\n            \"hemisphere\": self.hemisphere,\n            \"spine\": self.spine,\n            \"disposition\": self.disposition,\n            \"activity_class\": self.activity_class,\n            \"size\": self.size,\n            \"suffix\": self.suffix,\n            \"conductive_capable\": self.conductive_capable,\n            \"synthetic\": self.synthetic,\n        }\n        if include_path:\n            row[\"path\"] = self.path\n        return row\n\n\n@dataclass(frozen=True, slots=True)\nclass Fiber:\n    source: str\n    target: str\n    kind: str\n    conductive: bool\n    evidence: str\n\n    def as_dict(self) -> dict[str, Any]:\n        return {\n            \"source\": self.source,\n            \"target\": self.target,\n            \"kind\": self.kind,\n            \"conductive\": self.conductive,\n            \"evidence\": self.evidence,\n        }\n\n\nclass NeuralAtlas:\n    \"\"\"Read-only atlas: every inventory entry is addressable as a neuron.\"\"\"\n\n    def __init__(self, root: Path, *, inventory: Path | None = None) -> None:\n        self.root = root.expanduser().resolve()\n        self.inventory = inventory.expanduser().resolve() if inventory else None\n        self.inventory_source = \"\"\n        self.neurons: dict[str, Neuron] = {}\n        self.by_logical: dict[str, str] = {}\n        self.clusters: dict[str, list[str]] = defaultdict(list)\n        self.fibers: list[Fiber] = []\n        self._fiber_keys: set[tuple[str, str, str]] = set()\n        self._module_index: dict[str, str] = {}\n        self._built = False\n\n    def _source_rows(self) -> Iterable[dict[str, Any]]:\n        if self.inventory and self.inventory.is_file():\n            self.inventory_source = str(self.inventory)\n            return inventory_rows(self.inventory)\n        for candidate in inventory_candidates(self.root):\n            if candidate.is_file():\n                self.inventory_source = str(candidate)\n                return inventory_rows(candidate)\n        self.inventory_source = \"filesystem_scan_fallback\"\n        return _filesystem_rows(self.root)\n\n    def build(self) -> \"NeuralAtlas\":\n        if self._built:\n            return self\n        for row in self._source_rows():\n            raw = _entry_path(row)\n            if not raw:\n                continue\n            logical = _logical_path(raw, self.root)\n            if not logical:\n                continue\n            neuron_id = stable_neuron_id(raw)\n            if neuron_id in self.neurons:\n                continue\n            kind = _entry_kind(row)\n            disposition = _disposition(row)\n            cluster = _cluster_for(logical)\n            hemisphere, spine = _anatomy_for(cluster)\n            suffix = PurePosixPath(logical).suffix.casefold() if kind == \"file\" else \"\"\n            try:\n                size = int(row.get(\"size\") or row.get(\"bytes\") or 0)\n            except (TypeError, ValueError):\n                size = 0\n            neuron = Neuron(\n                id=neuron_id,\n                path=raw,\n                logical_path=logical,\n                kind=kind,\n                cluster=cluster,\n                hemisphere=hemisphere,\n                spine=spine,\n                disposition=disposition,\n                activity_class=_activity_class(disposition, logical),\n                size=max(0, size),\n                suffix=suffix,\n                conductive_capable=kind == \"file\" and suffix in _CODE_SUFFIXES,\n            )\n            self.neurons[neuron_id] = neuron\n            self.by_logical[logical] = neuron_id\n            self.clusters[cluster].append(neuron_id)\n        self._add_cluster_neurons()\n        self._index_modules()\n        self._add_containment_fibers()\n        self._add_python_import_fibers()\n        self._built = True\n        return self\n\n    def _add_cluster_neurons(self) -> None:\n        for cluster in tuple(sorted(self.clusters)):\n            logical = f\"@cluster/{cluster}\"\n            nid = stable_neuron_id(logical)\n            hemisphere, spine = _anatomy_for(cluster)\n            neuron = Neuron(\n                id=nid,\n                path=logical,\n                logical_path=logical,\n                kind=\"cluster\",\n                cluster=cluster,\n                hemisphere=hemisphere,\n                spine=spine,\n                disposition=\"synthetic_anatomical_cluster\",\n                activity_class=\"cluster\",\n                size=0,\n                suffix=\"\",\n                conductive_capable=False,\n                synthetic=True,\n            )\n            self.neurons[nid] = neuron\n            self.by_logical[logical] = nid\n\n    def _physical_path(self, neuron: Neuron) -> Path | None:\n        if neuron.synthetic or neuron.kind != \"file\":\n            return None\n        direct = Path(neuron.path)\n        if direct.is_file():\n            return direct\n        candidate = self.root / neuron.logical_path\n        return candidate if candidate.is_file() else None\n\n    @staticmethod\n    def _module_name(logical: str) -> str | None:\n        path = PurePosixPath(logical)\n        if path.suffix != \".py\":\n            return None\n        parts = list(path.with_suffix(\"\").parts)\n        if parts and parts[-1] == \"__init__\":\n            parts.pop()\n        return \".\".join(parts) if parts else None\n\n    def _index_modules(self) -> None:\n        for neuron in self.neurons.values():\n            module = self._module_name(neuron.logical_path)\n            if module:\n                self._module_index.setdefault(module, neuron.id)\n\n    def _add_fiber(self, source: str, target: str, kind: str, conductive: bool, evidence: str) -> None:\n        if source == target or source not in self.neurons or target not in self.neurons:\n            return\n        key = (source, target, kind)\n        if key in self._fiber_keys:\n            return\n        self._fiber_keys.add(key)\n        self.fibers.append(Fiber(source, target, kind, conductive, evidence))\n\n    def _add_containment_fibers(self) -> None:\n        for cluster, ids in self.clusters.items():\n            cluster_id = self.by_logical.get(f\"@cluster/{cluster}\")\n            if not cluster_id:\n                continue\n            for nid in ids:\n                self._add_fiber(\n                    cluster_id,\n                    nid,\n                    \"anatomical_membership\",\n                    False,\n                    \"inventory_cluster_membership\",\n                )\n\n    def _resolve_import(self, module: str) -> str | None:\n        candidate = module.strip(\".\")\n        while candidate:\n            if candidate in self._module_index:\n                return self._module_index[candidate]\n            candidate = candidate.rsplit(\".\", 1)[0] if \".\" in candidate else \"\"\n        return None\n\n    def _import_modules(self, tree: ast.AST, source_module: str) -> set[str]:\n        found: set[str] = set()\n        package = source_module.rsplit(\".\", 1)[0] if \".\" in source_module else \"\"\n        for node in ast.walk(tree):\n            if isinstance(node, ast.Import):\n                found.update(alias.name for alias in node.names)\n            elif isinstance(node, ast.ImportFrom):\n                base = node.module or \"\"\n                if node.level:\n                    parts = package.split(\".\") if package else []\n                    keep = max(0, len(parts) - node.level + 1)\n                    prefix = \".\".join(parts[:keep])\n                    base = \".\".join(x for x in (prefix, base) if x)\n                if base:\n                    found.add(base)\n                    for alias in node.names:\n                        if alias.name != \"*\":\n                            found.add(f\"{base}.{alias.name}\")\n        return found\n\n    def _add_python_import_fibers(self) -> None:\n        for neuron in tuple(self.neurons.values()):\n            source_module = self._module_name(neuron.logical_path)\n            if not source_module:\n                continue\n            path = self._physical_path(neuron)\n            if path is None:\n                continue\n            try:\n                tree = ast.parse(path.read_text(encoding=\"utf-8\", errors=\"replace\"), filename=str(path))\n            except (OSError, SyntaxError, UnicodeError):\n                continue\n            for module in self._import_modules(tree, source_module):\n                target = self._resolve_import(module)\n                if target:\n                    self._add_fiber(\n                        neuron.id,\n                        target,\n                        \"python_import\",\n                        True,\n                        f\"ast_import:{module}\",\n                    )\n\n    @property\n    def conductive_fibers(self) -> tuple[Fiber, ...]:\n        return tuple(f for f in self.fibers if f.conductive and f.kind in _CONDUCTIVE_KINDS)\n\n    def overview(self) -> dict[str, Any]:\n        self.build()\n        inventory_nodes = [n for n in self.neurons.values() if not n.synthetic]\n        cluster_rows = []\n        for cluster in sorted(self.clusters):\n            ids = self.clusters[cluster]\n            rows = [self.neurons[nid] for nid in ids]\n            dispositions = Counter(n.disposition for n in rows)\n            classes = Counter(n.activity_class for n in rows)\n            cluster_node = self.neurons[self.by_logical[f\"@cluster/{cluster}\"]]\n            cluster_rows.append(\n                {\n                    \"id\": cluster_node.id,\n                    \"cluster\": cluster,\n                    \"hemisphere\": cluster_node.hemisphere,\n                    \"spine\": cluster_node.spine,\n                    \"neuron_count\": len(rows),\n                    \"file_count\": sum(1 for n in rows if n.kind == \"file\"),\n                    \"code_count\": sum(1 for n in rows if n.conductive_capable),\n                    \"bytes\": sum(n.size for n in rows),\n                    \"dispositions\": dict(dispositions),\n                    \"activity_classes\": dict(classes),\n                }\n            )\n        return {\n            \"ok\": True,\n            \"schema\": \"eira2_neural_atlas_v1\",\n            \"inventory_source\": self.inventory_source,\n            \"inventory_neuron_count\": len(inventory_nodes),\n            \"synthetic_cluster_count\": len(self.clusters),\n            \"total_addressable_nodes\": len(self.neurons),\n            \"fiber_count\": len(self.fibers),\n            \"conductive_fiber_count\": len(self.conductive_fibers),\n            \"all_inventory_entries_have_neurons\": True,\n            \"structural_fibers_are_conductive\": False,\n            \"conductive_fibers_require_evidence\": True,\n            \"clusters\": cluster_rows,\n        }\n\n    def page(\n        self,\n        *,\n        cluster: str | None = None,\n        cursor: int = 0,\n        limit: int = 1500,\n        include_paths: bool = True,\n    ) -> dict[str, Any]:\n        self.build()\n        limit = max(1, min(5000, int(limit)))\n        cursor = max(0, int(cursor))\n        if cluster:\n            ids = list(self.clusters.get(cluster, ()))\n        else:\n            ids = [nid for nid, n in self.neurons.items() if not n.synthetic]\n        ids.sort(key=lambda nid: self.neurons[nid].logical_path)\n        selected = ids[cursor : cursor + limit]\n        end = cursor + len(selected)\n        return {\n            \"ok\": True,\n            \"schema\": \"eira2_neural_page_v1\",\n            \"cluster\": cluster,\n            \"cursor\": cursor,\n            \"next_cursor\": end if end < len(ids) else None,\n            \"total\": len(ids),\n            \"neurons\": [self.neurons[nid].as_dict(include_path=include_paths) for nid in selected],\n        }\n\n    def fibers_for(self, *, cluster: str | None = None, limit: int = 20000) -> dict[str, Any]:\n        self.build()\n        limit = max(1, min(50000, int(limit)))\n        if cluster:\n            allowed = set(self.clusters.get(cluster, ()))\n            cluster_id = self.by_logical.get(f\"@cluster/{cluster}\")\n            if cluster_id:\n                allowed.add(cluster_id)\n            rows = [f for f in self.fibers if f.source in allowed and f.target in allowed]\n        else:\n            rows = list(self.fibers)\n        truncated = len(rows) > limit\n        rows = rows[:limit]\n        return {\n            \"ok\": True,\n            \"schema\": \"eira2_neural_fibers_v1\",\n            \"cluster\": cluster,\n            \"count\": len(rows),\n            \"truncated\": truncated,\n            \"fibers\": [f.as_dict() for f in rows],\n        }\n\n    def search(self, query: str, *, limit: int = 100) -> dict[str, Any]:\n        self.build()\n        clean = \" \".join(str(query or \"\").casefold().split())\n        if not clean:\n            return {\"ok\": True, \"query\": \"\", \"matches\": []}\n        limit = max(1, min(500, int(limit)))\n        matches = [\n            n for n in self.neurons.values()\n            if clean in n.logical_path.casefold() or clean in n.cluster.casefold()\n        ]\n        matches.sort(key=lambda n: (n.synthetic, len(n.logical_path), n.logical_path))\n        return {\n            \"ok\": True,\n            \"schema\": \"eira2_neural_search_v1\",\n            \"query\": clean,\n            \"matches\": [n.as_dict() for n in matches[:limit]],\n        }\n\n    def neuron(self, neuron_id: str) -> dict[str, Any] | None:\n        self.build()\n        row = self.neurons.get(str(neuron_id or \"\"))\n        if row is None:\n            return None\n        incoming = sum(1 for f in self.fibers if f.target == row.id)\n        outgoing = sum(1 for f in self.fibers if f.source == row.id)\n        conductive = sum(1 for f in self.fibers if f.conductive and row.id in {f.source, f.target})\n        value = row.as_dict()\n        value.update({\"incoming_fibers\": incoming, \"outgoing_fibers\": outgoing, \"conductive_fibers\": conductive})\n        return value\n"
      },
      {
        "bytes": 16133,
        "path": "eira2/neural/communication.py",
        "sha256": "58ffcb1ccc2a4b22b15a7eec5a8f9fa6c3b6fbe0978b99306b210b6e034a6bee",
        "source": "from __future__ import annotations\n\nimport asyncio\nimport json\nimport threading\nfrom collections.abc import Awaitable, Callable\nfrom typing import Any\nfrom urllib.parse import parse_qs, urlsplit\n\nfrom ..contracts import Event, RuntimeContext, ServiceState\nfrom ..conversation.spine import ConversationSpine\nfrom ..delivery.gate import DeliveryGate\nfrom ..delivery.presence import ReactivePresenceService\nfrom ..interfaces.ingress import IngressService, InputKind\nfrom ..interfaces.native import NativeInputEvidenceService\nfrom ..kernel.events import EventService\nfrom .live import LiveNeuralHTTPService\nfrom .server import NeuralHandler, NeuralHTTPServer\n\n\nTextSubmitter = Callable[[str], Awaitable[Any]]\nPCMSubmitter = Callable[[bytes, int, int, int], Awaitable[Any]]\nAmbientSubmitter = Callable[[bytes, int, int, int], Awaitable[dict[str, Any]]]\nLocationSubmitter = Callable[[float, float, float, str], Awaitable[dict[str, Any]]]\n\n\nclass CommunicationHandler(NeuralHandler):\n    \"\"\"Canonical text, explicit voice and wake-gated ambient ingress.\"\"\"\n\n    _TEXT_LIMIT = 256 * 1024\n    _PCM_LIMIT = 32 * 1024 * 1024\n    _AMBIENT_PCM_LIMIT = 12 * 1024 * 1024\n    _LOCATION_LIMIT = 16 * 1024\n\n    def _security_headers(self) -> None:\n        self.send_header(\"X-Content-Type-Options\", \"nosniff\")\n        self.send_header(\"Referrer-Policy\", \"no-referrer\")\n        self.send_header(\"Cross-Origin-Opener-Policy\", \"same-origin\")\n        self.send_header(\n            \"Permissions-Policy\",\n            \"microphone=(self), camera=(self), geolocation=(self), accelerometer=(self), gyroscope=(self)\",\n        )\n        self.send_header(\n            \"Content-Security-Policy\",\n            \"default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:; \"\n            \"connect-src 'self'; media-src 'self'; object-src 'none'; base-uri 'none'; frame-ancestors 'none'\",\n        )\n\n    def _read_body(self, limit: int) -> bytes:\n        try:\n            length = int(self.headers.get(\"Content-Length\") or \"0\")\n        except ValueError as error:\n            raise ValueError(\"content_length_invalid\") from error\n        if length <= 0:\n            raise ValueError(\"request_body_required\")\n        if length > limit:\n            raise ValueError(\"request_body_too_large\")\n        return self.rfile.read(length)\n\n    def _authorized_peer(self) -> dict[str, Any] | None:\n        peer = str(self.client_address[0] if self.client_address else \"\")\n        receipt = NativeInputEvidenceService.secure_peer(peer)\n        if not receipt.get(\"ok\"):\n            self._json(403, {\"ok\": False, \"error\": \"remote_peer_not_authorized\"})\n            return None\n        return receipt\n\n    @staticmethod\n    def _turn_payload(result: Any) -> dict[str, Any]:\n        return {\n            \"ok\": bool(getattr(result, \"verified\", False)),\n            \"verified\": bool(getattr(result, \"verified\", False)),\n            \"correlation_id\": str(getattr(result, \"correlation_id\", \"\")),\n            \"response\": str(getattr(result, \"response\", \"\")),\n        }\n\n    @staticmethod\n    def _pcm_parameters(parsed: Any) -> tuple[int, int, int]:\n        query = parse_qs(parsed.query, keep_blank_values=False)\n        sample_rate = int((query.get(\"sample_rate\") or [\"16000\"])[0])\n        channels = int((query.get(\"channels\") or [\"1\"])[0])\n        sample_width = int((query.get(\"sample_width\") or [\"2\"])[0])\n        if not 8000 <= sample_rate <= 192000:\n            raise ValueError(\"sample_rate_out_of_range\")\n        if channels not in {1, 2}:\n            raise ValueError(\"channels_out_of_range\")\n        if sample_width not in {1, 2, 3, 4}:\n            raise ValueError(\"sample_width_out_of_range\")\n        return sample_rate, channels, sample_width\n\n    def _submit(self, coroutine: Awaitable[Any], timeout_seconds: float | None = None) -> Any:\n        loop = getattr(self.server, \"eira_loop\", None)\n        if loop is None:\n            raise RuntimeError(\"communication_event_loop_missing\")\n        future = asyncio.run_coroutine_threadsafe(coroutine, loop)\n        return future.result(timeout=timeout_seconds)\n\n    def do_POST(self) -> None:\n        peer = self._authorized_peer()\n        if peer is None:\n            return\n        parsed = urlsplit(self.path)\n        try:\n            if parsed.path == \"/v1/text\":\n                raw = self._read_body(self._TEXT_LIMIT)\n                payload = json.loads(raw.decode(\"utf-8\"))\n                if not isinstance(payload, dict):\n                    raise ValueError(\"text_request_must_be_object\")\n                text = str(payload.get(\"text\") or \"\").strip()\n                if not text:\n                    raise ValueError(\"text_required\")\n                submitter = getattr(self.server, \"text_submitter\", None)\n                if not callable(submitter):\n                    raise RuntimeError(\"text_submitter_unavailable\")\n                result = self._submit(submitter(text))\n                return self._json(200, self._turn_payload(result))\n\n            if parsed.path == \"/v1/listen\":\n                raw = self._read_body(self._PCM_LIMIT)\n                sample_rate, channels, sample_width = self._pcm_parameters(parsed)\n                submitter = getattr(self.server, \"pcm_submitter\", None)\n                if not callable(submitter):\n                    raise RuntimeError(\"pcm_submitter_unavailable\")\n                spoken = self._submit(submitter(raw, sample_rate, channels, sample_width))\n                if not isinstance(spoken, dict) or \"result\" not in spoken:\n                    raise RuntimeError(\"pcm_submitter_contract_invalid\")\n                payload = self._turn_payload(spoken[\"result\"])\n                payload[\"utterance\"] = str(spoken.get(\"utterance\") or \"\")\n                return self._json(200, payload)\n\n            if parsed.path == \"/v1/ambient\":\n                raw = self._read_body(self._AMBIENT_PCM_LIMIT)\n                sample_rate, channels, sample_width = self._pcm_parameters(parsed)\n                submitter = getattr(self.server, \"ambient_submitter\", None)\n                if not callable(submitter):\n                    raise RuntimeError(\"ambient_submitter_unavailable\")\n                result = self._submit(submitter(raw, sample_rate, channels, sample_width))\n                return self._json(200, dict(result))\n\n            if parsed.path == \"/v1/location\":\n                raw = self._read_body(self._LOCATION_LIMIT)\n                payload = json.loads(raw.decode(\"utf-8\"))\n                if not isinstance(payload, dict):\n                    raise ValueError(\"location_request_must_be_object\")\n                latitude = float(payload.get(\"latitude\"))\n                longitude = float(payload.get(\"longitude\"))\n                accuracy_m = float(payload.get(\"accuracy_m\") or 0.0)\n                submitter = getattr(self.server, \"location_submitter\", None)\n                if not callable(submitter):\n                    raise RuntimeError(\"location_submitter_unavailable\")\n                result = self._submit(\n                    submitter(latitude, longitude, accuracy_m, str(peer.get(\"transport\") or \"\")),\n                    timeout_seconds=30.0,\n                )\n                safe = dict(result)\n                safe.pop(\"latitude\", None)\n                safe.pop(\"longitude\", None)\n                return self._json(200, {\"ok\": True, **safe})\n        except (UnicodeError, json.JSONDecodeError, TypeError, ValueError) as error:\n            return self._json(400, {\"ok\": False, \"error\": f\"{type(error).__name__}:{error}\"[:500]})\n        except TimeoutError:\n            return self._json(504, {\"ok\": False, \"error\": \"canonical_turn_timeout\"})\n        except BaseException as error:\n            return self._json(503, {\"ok\": False, \"error\": f\"{type(error).__name__}:{error}\"[:500]})\n        self._json(404, {\"ok\": False, \"error\": \"not_found\"})\n\n\nclass CommunicationHTTPServer(NeuralHTTPServer):\n    def __init__(\n        self,\n        address: tuple[str, int],\n        surface: Any,\n        *,\n        loop: asyncio.AbstractEventLoop,\n        text_submitter: TextSubmitter,\n        pcm_submitter: PCMSubmitter,\n        ambient_submitter: AmbientSubmitter,\n        location_submitter: LocationSubmitter,\n    ) -> None:\n        self.surface = surface\n        self.eira_loop = loop\n        self.text_submitter = text_submitter\n        self.pcm_submitter = pcm_submitter\n        self.ambient_submitter = ambient_submitter\n        self.location_submitter = location_submitter\n        super(NeuralHTTPServer, self).__init__(address, CommunicationHandler)\n\n\nclass CommunicationLiveNeuralHTTPService(LiveNeuralHTTPService):\n    \"\"\"One HTTP server for neural UI, Apple AR surface and canonical iPhone ingress.\"\"\"\n\n    def __init__(\n        self,\n        *args: Any,\n        text_submitter: TextSubmitter | None = None,\n        pcm_submitter: PCMSubmitter | None = None,\n        ambient_submitter: AmbientSubmitter | None = None,\n        location_submitter: LocationSubmitter | None = None,\n        **kwargs: Any,\n    ) -> None:\n        super().__init__(*args, **kwargs)\n        self._text_submitter = text_submitter\n        self._pcm_submitter = pcm_submitter\n        self._ambient_submitter = ambient_submitter\n        self._location_submitter = location_submitter\n\n    @staticmethod\n    def _canonical_submitters(\n        context: RuntimeContext,\n    ) -> tuple[TextSubmitter, PCMSubmitter, AmbientSubmitter, LocationSubmitter]:\n        ingress = context.get_service(\"interfaces.ingress\")\n        native = context.get_service(\"interfaces.native\")\n        presence = context.get_service(\"delivery.reactive_presence\")\n        conversation = context.get_service(\"conversation.spine\")\n        delivery = context.get_service(\"delivery.gate\")\n        if not isinstance(ingress, IngressService):\n            raise RuntimeError(\"canonical_ingress_missing\")\n        if not isinstance(native, NativeInputEvidenceService):\n            raise RuntimeError(\"canonical_native_input_missing\")\n        if not isinstance(presence, ReactivePresenceService):\n            raise RuntimeError(\"canonical_reactive_presence_missing\")\n        if not isinstance(conversation, ConversationSpine):\n            raise RuntimeError(\"canonical_conversation_spine_missing\")\n        if not isinstance(delivery, DeliveryGate):\n            raise RuntimeError(\"canonical_delivery_gate_missing\")\n\n        async def submit_turn(text: str, kind: InputKind) -> Any:\n            clean_text = str(text or \"\").strip()\n            if not clean_text:\n                raise ValueError(\"empty_turn\")\n            envelope = ingress.accept(clean_text, kind)\n            await presence.begin(envelope.correlation_id, mode=\"thinking\")\n            try:\n                result = await conversation.respond(envelope.text, envelope.correlation_id)\n                await delivery.deliver(result.correlation_id, result.response)\n                return result\n            finally:\n                await presence.end(envelope.correlation_id)\n\n        async def text_submitter(text: str) -> Any:\n            return await submit_turn(text, InputKind.IPHONE_TEXT)\n\n        async def pcm_submitter(raw: bytes, sample_rate: int, channels: int, sample_width: int) -> dict[str, Any]:\n            transcript = await native.transcribe_pcm(raw, sample_rate=sample_rate, channels=channels, sample_width=sample_width)\n            wake = native.wake_word(str(transcript[\"text\"]))\n            utterance = str(wake.get(\"utterance\") or \"\").strip()\n            if not utterance:\n                raise ValueError(\"voice_utterance_empty\")\n            result = await submit_turn(utterance, InputKind.IPHONE_VOICE)\n            return {\"result\": result, \"utterance\": utterance}\n\n        async def ambient_submitter(raw: bytes, sample_rate: int, channels: int, sample_width: int) -> dict[str, Any]:\n            transcript = await native.transcribe_pcm(raw, sample_rate=sample_rate, channels=channels, sample_width=sample_width)\n            wake = native.wake_word(str(transcript[\"text\"]))\n            if not bool(wake.get(\"invoked\")):\n                return {\"ok\": True, \"invoked\": False, \"raw_audio_retained\": False, \"transcript_retained\": False}\n            utterance = str(wake.get(\"utterance\") or \"\").strip()\n            if not utterance:\n                return {\"ok\": True, \"invoked\": True, \"awaiting_utterance\": True, \"raw_audio_retained\": False, \"transcript_retained\": False}\n            result = await submit_turn(utterance, InputKind.IPHONE_VOICE)\n            return {\n                \"ok\": bool(getattr(result, \"verified\", False)),\n                \"invoked\": True,\n                \"verified\": bool(getattr(result, \"verified\", False)),\n                \"correlation_id\": str(getattr(result, \"correlation_id\", \"\")),\n                \"response\": str(getattr(result, \"response\", \"\")),\n                \"utterance\": utterance,\n                \"raw_audio_retained\": False,\n                \"transcript_retained\": False,\n            }\n\n        async def location_submitter(latitude: float, longitude: float, accuracy_m: float, transport: str) -> dict[str, Any]:\n            local_context = context.get_service(\"extension.local_context\")\n            updater = getattr(local_context, \"update_location\", None)\n            if not callable(updater):\n                raise RuntimeError(\"canonical_local_context_location_update_unavailable\")\n            return await updater(latitude=latitude, longitude=longitude, accuracy_m=accuracy_m, source_transport=transport)\n\n        return text_submitter, pcm_submitter, ambient_submitter, location_submitter\n\n    async def start(self, context: RuntimeContext) -> None:\n        if not self._subscriptions_installed:\n            events = context.get_service(\"kernel.events\")\n            if isinstance(events, EventService):\n                for topic in (\n                    \"runtime.node_registered\", \"runtime.node_unregistered\", \"registry.node_registered\", \"registry.node_unregistered\",\n                    \"runtime.extension_registered\", \"runtime.extension_unregistered\", \"registry.extension_registered\", \"registry.extension_unregistered\",\n                    \"runtime.file_added\", \"runtime.file_removed\", \"runtime.file_changed\",\n                ):\n                    events.subscribe(topic, self._topology_event)\n                self._subscriptions_installed = True\n\n        text_submitter = self._text_submitter\n        pcm_submitter = self._pcm_submitter\n        ambient_submitter = self._ambient_submitter\n        location_submitter = self._location_submitter\n        if text_submitter is None or pcm_submitter is None or ambient_submitter is None or location_submitter is None:\n            text_submitter, pcm_submitter, ambient_submitter, location_submitter = self._canonical_submitters(context)\n\n        self.surface.start()\n        loop = asyncio.get_running_loop()\n        server = CommunicationHTTPServer(\n            (self.host, self.port), self.surface, loop=loop, text_submitter=text_submitter,\n            pcm_submitter=pcm_submitter, ambient_submitter=ambient_submitter, location_submitter=location_submitter,\n        )\n        self.port = int(server.server_address[1])\n        self._server = server\n        self._thread = threading.Thread(target=server.serve_forever, name=\"eira2-neural-communication-http\", kwargs={\"poll_interval\": 0.2}, daemon=True)\n        self._thread.start()\n        self._state = ServiceState.READY\n        await context.publish(Event(\"interfaces.communication.ready\", {\n            \"host\": self.host,\n            \"port\": self.port,\n            \"text_endpoint\": \"/v1/text\",\n            \"pcm_endpoint\": \"/v1/listen\",\n            \"ambient_endpoint\": \"/v1/ambient\",\n            \"ambient_policy\": \"local_vad_plus_wake_word_required_before_conversation\",\n            \"raw_audio_retained\": False,\n            \"location_endpoint\": \"/v1/location\",\n            \"location_privacy\": \"precise_coordinates_local_state_only\",\n            \"apple_ar_brain\": \"/inventions/usdz/eira2_brain.usdz\",\n            \"shared_server\": True,\n            \"single_conversation_authority\": \"conversation.spine\",\n            \"single_delivery_authority\": \"delivery.gate\",\n        }))"
      },
      {
        "bytes": 2375,
        "path": "eira2/neural/growth.py",
        "sha256": "3a4118e912e193ac8f258a65471b7108d829ba5a0763312edbe560bfb65a46a9",
        "source": "from __future__ import annotations\n\nfrom pathlib import Path, PurePosixPath\n\nfrom .atlas import (\n    NeuralAtlas,\n    Neuron,\n    _CODE_SUFFIXES,\n    _activity_class,\n    _anatomy_for,\n    _cluster_for,\n    _disposition,\n    _entry_kind,\n    _entry_path,\n    _filesystem_rows,\n    _logical_path,\n    stable_neuron_id,\n)\n\n\ndef overlay_live_filesystem(atlas: NeuralAtlas, root: Path) -> int:\n    \"\"\"Add post-ledger filesystem entries without duplicating canonical neurons.\n\n    The forensic migration ledger remains the authoritative baseline. A live\n    entry whose logical path already exists in that ledger is not duplicated.\n    New paths become additional neurons and receive the same structural/import\n    analysis as the baseline atlas.\n    \"\"\"\n    root = root.expanduser().resolve()\n    added = 0\n    for row in _filesystem_rows(root):\n        raw = _entry_path(row)\n        if not raw:\n            continue\n        logical = _logical_path(raw, root)\n        if not logical or logical in atlas.by_logical:\n            continue\n        kind = _entry_kind(row)\n        disposition = _disposition(row)\n        cluster = _cluster_for(logical)\n        hemisphere, spine = _anatomy_for(cluster)\n        suffix = PurePosixPath(logical).suffix.casefold() if kind == \"file\" else \"\"\n        try:\n            size = int(row.get(\"size\") or row.get(\"bytes\") or 0)\n        except (TypeError, ValueError):\n            size = 0\n        neuron_id = stable_neuron_id(raw)\n        if neuron_id in atlas.neurons:\n            continue\n        atlas.neurons[neuron_id] = Neuron(\n            id=neuron_id,\n            path=raw,\n            logical_path=logical,\n            kind=kind,\n            cluster=cluster,\n            hemisphere=hemisphere,\n            spine=spine,\n            disposition=\"eira2_live_growth\" if disposition == \"runtime_scan_fallback\" else disposition,\n            activity_class=_activity_class(\"migrate_runtime\", logical),\n            size=max(0, size),\n            suffix=suffix,\n            conductive_capable=kind == \"file\" and suffix in _CODE_SUFFIXES,\n        )\n        atlas.by_logical[logical] = neuron_id\n        atlas.clusters[cluster].append(neuron_id)\n        added += 1\n\n    if added:\n        atlas._add_cluster_neurons()\n        atlas._index_modules()\n        atlas._add_containment_fibers()\n        atlas._add_python_import_fibers()\n    return added\n"
      }
    ],
    "generated_unix": 1788830761.8412356,
    "matching_files": [],
    "mutates_live": false,
    "request_id": "neural_constructor_sources_v1",
    "schema": "eira2_transport_inspection_evidence_v2",
    "served": [],
    "watcher_authorization": {
      "authorized": true,
      "authorized_by": "repair_watcher_ai",
      "builder_required": false,
      "fast_read_only_authorization": true,
      "generated_unix": 1788830757.284758,
      "ok": true,
      "operation": "inspect",
      "read_only": true,
      "request_id": "neural_constructor_sources_v1",
      "superprobe_required_for_authorization": false,
      "watcher_version": "2.6.0-eira2-fast-readonly-transport-authority",
      "write_authority_granted": false
    }
  },
  "ok": true,
  "operation": "inspect",
  "request_id": "neural_constructor_sources_v1",
  "schema": "eira2_transport_terminal_receipt_v6",
  "status": "INSPECTION_COMPLETE",
  "transport_request_sha256": "d5347e8832b1374b70c47d97f48d21e31b1f15fd2ae29d5f5229df7a056eec7b",
  "watcher_authorization": {
    "authorized": true,
    "authorized_by": "repair_watcher_ai",
    "builder_required": false,
    "fast_read_only_authorization": true,
    "generated_unix": 1788830757.284758,
    "ok": true,
    "operation": "inspect",
    "read_only": true,
    "request_id": "neural_constructor_sources_v1",
    "superprobe_required_for_authorization": false,
    "watcher_version": "2.6.0-eira2-fast-readonly-transport-authority",
    "write_authority_granted": false
  },
  "watcher_authorized": true
}
