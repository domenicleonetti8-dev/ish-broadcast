#!/usr/bin/env python3
from __future__ import annotations

TARGET = "extensions/engineering_worker_ai/plugin.py"

NEW = 'from __future__ import annotations\n\nimport difflib\nimport errno\nimport fcntl\nimport hashlib\nimport json\nimport os\nimport shutil\nimport signal\nimport subprocess\nimport tempfile\nimport time\nimport urllib.request\nfrom contextlib import contextmanager\nfrom pathlib import Path\nfrom typing import Any\n\nSCHEMA = "eira2_engineering_worker_opencode_aider_v6_canonical_hardened"\nEXTENSION_ID = "eira.engineering.worker"\n\nOPENCODE_MODEL = "ollama/qwen2.5-coder:3b"\nAIDER_MODEL = "ollama_chat/qwen2.5-coder:3b"\nOLLAMA_MODEL = "qwen2.5-coder:3b"\n\nPINNED = {\n    "opencode": Path.home() / ".opencode/bin/opencode",\n    "aider": Path.home() / ".local/bin/aider",\n    "python3": Path("/usr/bin/python3"),\n    "git": Path("/usr/bin/git"),\n    "bwrap": Path("/usr/bin/bwrap"),\n}\n\nMAX_CHANGED_FILES = 64\nMAX_ALLOWED_PATHS = 64\nMAX_CANDIDATE_BYTES = 8_000_000\nMAX_PLAN_BYTES = 32_000\nMAX_REVIEW_BYTES = 8_000_000\nMAX_CAPTURE_BYTES = 2_000_000\nMAX_OBJECTIVE_BYTES = 32_000\nMAX_EVIDENCE_BYTES = 512_000\nMAX_TEST_COMMANDS = 32\nMAX_REPAIR_ATTEMPTS = 3\nSTAGE_TIMEOUTS = {"inspect": 300, "plan": 300, "aider": 600, "review": 300, "test": 300}\n\nSTATE_ROOT = Path.home() / ".local/state/eira_engineering_worker_v6"\nLOCK_PATH = STATE_ROOT / "worker.lock"\n\nTRUSTED_EVIDENCE_PREFIXES = ("eira_probe/", "eira2_transport_bus/from_superprobe/")\nAUTHORIZATION_PREFIX = "eira_probe/owner_authorizations/"\nPROTECTED_PREFIXES = (\n    "extensions/repair_watcher_ai/",\n    "tools/eira2_transaction_executor.py",\n    "tools/eira2_superprobe",\n)\n\ndef _sha(data: bytes) -> str:\n    return hashlib.sha256(data).hexdigest()\n\ndef _sha_file(path: Path) -> str:\n    h = hashlib.sha256()\n    with path.open("rb") as f:\n        for chunk in iter(lambda: f.read(1024 * 1024), b""):\n            h.update(chunk)\n    return h.hexdigest()\n\ndef _safe_rel(value: str) -> str:\n    if not isinstance(value, str) or not value.strip():\n        raise RuntimeError("empty_relative_path")\n    p = Path(value)\n    if value in {".", "./"} or p == Path(".") or p.is_absolute() or ".." in p.parts:\n        raise RuntimeError("unsafe_relative_path:" + value)\n    rel = p.as_posix()\n    if rel.startswith("/") or rel.startswith("../") or "/../" in rel:\n        raise RuntimeError("unsafe_relative_path:" + value)\n    return rel\n\ndef _safe_job_id(value: Any) -> str:\n    s = str(value or "").strip()\n    allowed = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789._-"\n    if not s or len(s) > 96 or any(c not in allowed for c in s):\n        raise RuntimeError("valid_job_id_required")\n    return s\n\ndef _bounded_text(value: Any, limit: int, label: str) -> str:\n    s = str(value or "").strip()\n    if not s:\n        raise RuntimeError(label + "_required")\n    if len(s.encode("utf-8")) > limit:\n        raise RuntimeError(label + "_too_large")\n    return s\n\ndef _binary(name: str) -> str:\n    p = PINNED[name]\n    if not p.is_file() or p.is_symlink() or not os.access(p, os.X_OK):\n        raise RuntimeError(name + "_binary_unavailable:" + str(p))\n    return str(p)\n\ndef _tool_receipt(name: str) -> dict[str, Any]:\n    p = Path(_binary(name))\n    st = p.stat()\n    return {"path": str(p), "sha256": _sha_file(p), "size": st.st_size, "mode": st.st_mode & 0o777}\n\ndef _tool_set() -> dict[str, dict[str, Any]]:\n    return {name: _tool_receipt(name) for name in PINNED}\n\ndef _assert_tool_set(expected: dict[str, dict[str, Any]]) -> None:\n    if _tool_set() != expected:\n        raise RuntimeError("engineering_tool_identity_changed_during_job")\n\ndef _ollama_digest(model: str = OLLAMA_MODEL) -> str:\n    req = urllib.request.Request("http://127.0.0.1:11434/api/tags", method="GET")\n    with urllib.request.urlopen(req, timeout=5) as resp:\n        raw = resp.read(2_000_000)\n    obj = json.loads(raw.decode("utf-8"))\n    for row in obj.get("models") or []:\n        name = str(row.get("name") or row.get("model") or "")\n        if name == model:\n            digest = str(row.get("digest") or "")\n            if len(digest) >= 32:\n                return digest\n    raise RuntimeError("ollama_model_digest_unavailable:" + model)\n\ndef _clean_env() -> dict[str, str]:\n    return {\n        "PATH": "/usr/bin:/bin",\n        "LANG": os.environ.get("LANG", "C.UTF-8"),\n        "HOME": "/home/eira",\n        "TMPDIR": "/tmp",\n        "PYTHONUNBUFFERED": "1",\n        "PYTHONDONTWRITEBYTECODE": "1",\n        "AIDER_YES_ALWAYS": "true",\n        "AIDER_AUTO_COMMITS": "false",\n        "EIRA_ENGINEERING_WORKSPACE_ONLY": "1",\n        "EIRA_ENGINEERING_LIVE_WRITE": "0",\n        "OLLAMA_HOST": "http://127.0.0.1:11434",\n    }\n\ndef _bwrap_prefix(workspace: Path, *, network: bool) -> list[str]:\n    argv = [\n        _binary("bwrap"), "--die-with-parent", "--new-session",\n        "--unshare-pid", "--unshare-ipc", "--unshare-uts",\n        "--proc", "/proc", "--dev", "/dev", "--tmpfs", "/tmp",\n        "--dir", "/home", "--dir", "/home/eira",\n    ]\n    if not network:\n        argv.append("--unshare-net")\n    for p in ("/usr", "/bin", "/lib", "/lib64", "/etc"):\n        if Path(p).exists():\n            argv += ["--ro-bind", p, p]\n    home = Path.home()\n    # Tool trees are read-only. The host HOME itself is never exposed as HOME.\n    for p in (home / ".opencode", home / ".local"):\n        if p.exists():\n            argv += ["--ro-bind", str(p), str(p)]\n    ocfg = home / ".config/opencode"\n    if ocfg.exists():\n        argv += ["--ro-bind", str(ocfg), str(ocfg)]\n    argv += ["--bind", str(workspace), "/workspace", "--chdir", "/workspace"]\n    return argv\n\ndef _kill_group(pid: int) -> None:\n    for sig in (signal.SIGTERM, signal.SIGKILL):\n        try:\n            os.killpg(pid, sig)\n        except ProcessLookupError:\n            return\n        except Exception:\n            pass\n        time.sleep(0.05)\n\ndef _read_tail(path: Path, limit: int = MAX_CAPTURE_BYTES) -> str:\n    if not path.exists():\n        return ""\n    with path.open("rb") as f:\n        size = f.seek(0, os.SEEK_END)\n        f.seek(max(0, size - limit), os.SEEK_SET)\n        return f.read(limit).decode("utf-8", errors="replace")\n\ndef _run(\n    argv: list[str],\n    workspace: Path,\n    timeout: int,\n    *,\n    contained: bool = True,\n    network: bool = False,\n    capture_limit: int = MAX_CAPTURE_BYTES,\n) -> dict[str, Any]:\n    timeout = max(1, min(int(timeout), 3600))\n    started = time.monotonic()\n    out_fd, out_name = tempfile.mkstemp(prefix="eira-v6-out-", dir="/tmp")\n    err_fd, err_name = tempfile.mkstemp(prefix="eira-v6-err-", dir="/tmp")\n    os.close(out_fd)\n    os.close(err_fd)\n    out, err = Path(out_name), Path(err_name)\n    proc: subprocess.Popen[bytes] | None = None\n    timed_out = False\n    overflow = False\n    rc = 1\n    try:\n        cmd = (_bwrap_prefix(workspace, network=network) + ["--"] + argv) if contained else argv\n        with out.open("wb") as fo, err.open("wb") as fe:\n            proc = subprocess.Popen(\n                cmd, cwd=str(workspace), stdin=subprocess.DEVNULL,\n                stdout=fo, stderr=fe, env=_clean_env(), start_new_session=True,\n            )\n            while True:\n                polled = proc.poll()\n                if polled is not None:\n                    rc = int(polled)\n                    break\n                if time.monotonic() - started > timeout:\n                    timed_out = True\n                    rc = 124\n                    _kill_group(proc.pid)\n                    break\n                if out.stat().st_size > capture_limit or err.stat().st_size > capture_limit:\n                    overflow = True\n                    rc = 125\n                    _kill_group(proc.pid)\n                    break\n                time.sleep(0.05)\n            try:\n                proc.wait(timeout=5)\n            except subprocess.TimeoutExpired:\n                _kill_group(proc.pid)\n                try:\n                    proc.wait(timeout=2)\n                except subprocess.TimeoutExpired:\n                    pass\n            _kill_group(proc.pid)\n        return {\n            "argv": argv,\n            "returncode": 124 if timed_out else (125 if overflow else rc),\n            "timed_out": timed_out,\n            "output_overflow": overflow,\n            "stdout": _read_tail(out),\n            "stderr": _read_tail(err),\n            "elapsed_seconds": round(time.monotonic() - started, 3),\n        }\n    finally:\n        if proc is not None and proc.poll() is None:\n            _kill_group(proc.pid)\n        out.unlink(missing_ok=True)\n        err.unlink(missing_ok=True)\n\n@contextmanager\ndef _worker_lock():\n    STATE_ROOT.mkdir(parents=True, exist_ok=True)\n    with LOCK_PATH.open("a+") as f:\n        try:\n            fcntl.flock(f.fileno(), fcntl.LOCK_EX | fcntl.LOCK_NB)\n        except BlockingIOError:\n            raise RuntimeError("engineering_worker_already_running")\n        yield\n\ndef _job_dir(job_id: str) -> Path:\n    path = STATE_ROOT / "jobs" / _safe_job_id(job_id)\n    path.mkdir(parents=True, exist_ok=True)\n    return path\n\ndef _append_receipt(job_dir: Path, ledger: list[dict[str, Any]], stage: str, status: str, **extra: Any) -> None:\n    row = {"schema": SCHEMA, "stage": stage, "status": status, "unix": time.time(), **extra}\n    ledger.append(row)\n    p = job_dir / "ledger.jsonl"\n    with p.open("a", encoding="utf-8") as f:\n        f.write(json.dumps(row, sort_keys=True) + "\\n")\n        f.flush()\n        os.fsync(f.fileno())\n\ndef _completed_receipt(job_dir: Path) -> dict[str, Any] | None:\n    p = job_dir / "completed.json"\n    return json.loads(p.read_text(encoding="utf-8")) if p.is_file() else None\n\ndef _seal_completed(job_dir: Path, payload: dict[str, Any]) -> None:\n    tmp, final = job_dir / "completed.json.tmp", job_dir / "completed.json"\n    tmp.write_text(json.dumps(payload, indent=2, sort_keys=True), encoding="utf-8")\n    os.replace(tmp, final)\n\ndef _manifest(root: Path) -> tuple[list[dict[str, Any]], str]:\n    p = root / "eira2-package-manifest.json"\n    if not p.is_file() or p.is_symlink():\n        raise RuntimeError("canonical_package_manifest_unavailable")\n    raw = p.read_bytes()\n    obj = json.loads(raw.decode("utf-8"))\n    rows = obj.get("files")\n    if not isinstance(rows, list) or not rows:\n        raise RuntimeError("canonical_package_manifest_empty")\n    seen: set[str] = set()\n    clean: list[dict[str, Any]] = []\n    for row in rows:\n        if not isinstance(row, dict):\n            raise RuntimeError("canonical_package_manifest_invalid_row")\n        rel = _safe_rel(str(row.get("path") or ""))\n        if rel in seen:\n            raise RuntimeError("canonical_package_manifest_duplicate_path:" + rel)\n        seen.add(rel)\n        digest, size = row.get("sha256"), row.get("size")\n        if not isinstance(digest, str) or len(digest) != 64 or any(c not in "0123456789abcdefABCDEF" for c in digest):\n            raise RuntimeError("canonical_manifest_sha_required:" + rel)\n        if not isinstance(size, int) or size < 0:\n            raise RuntimeError("canonical_manifest_size_required:" + rel)\n        clean.append({"path": rel, "sha256": digest.lower(), "size": size})\n    return clean, _sha(raw)\n\ndef _capture(root: Path, rows: list[dict[str, Any]]) -> dict[str, dict[str, Any]]:\n    state: dict[str, dict[str, Any]] = {}\n    for row in rows:\n        rel = row["path"]\n        p = root / rel\n        if p.is_symlink() or not p.is_file():\n            raise RuntimeError("canonical_source_missing_or_symlink:" + rel)\n        a = p.stat()\n        digest = _sha_file(p)\n        b = p.stat()\n        if (a.st_size, a.st_mtime_ns, a.st_ino) != (b.st_size, b.st_mtime_ns, b.st_ino):\n            raise RuntimeError("canonical_source_changed_while_reading:" + rel)\n        if digest != row["sha256"] or b.st_size != row["size"]:\n            raise RuntimeError("canonical_manifest_mismatch:" + rel)\n        state[rel] = {"type": "file", "sha256": digest, "size": b.st_size, "mode": b.st_mode & 0o777}\n    return state\n\ndef _copy_snapshot(root: Path, rows: list[dict[str, Any]], state: dict[str, Any], dst: Path) -> None:\n    for row in rows:\n        rel = row["path"]\n        src, out = root / rel, dst / rel\n        out.parent.mkdir(parents=True, exist_ok=True)\n        shutil.copy2(src, out, follow_symlinks=False)\n        if out.is_symlink() or not out.is_file():\n            raise RuntimeError("snapshot_type_violation:" + rel)\n        if _sha_file(out) != state[rel]["sha256"] or out.stat().st_size != state[rel]["size"]:\n            raise RuntimeError("snapshot_copy_mismatch:" + rel)\n    if _capture(root, rows) != state:\n        raise RuntimeError("canonical_source_drift_during_snapshot")\n\ndef _workspace(root: Path, job_dir: Path, ledger: list[dict[str, Any]]) -> dict[str, Any]:\n    session = Path(tempfile.mkdtemp(prefix="eira_engineering_v6_", dir="/tmp"))\n    try:\n        rows, manifest_sha = _manifest(root)\n        source = _capture(root, rows)\n        frozen = session / "frozen"\n        frozen.mkdir()\n        _copy_snapshot(root, rows, source, frozen)\n        source_fp = _sha(json.dumps(\n            {"manifest_sha256": manifest_sha, "files": source},\n            sort_keys=True, separators=(",", ":")\n        ).encode())\n        _append_receipt(\n            job_dir, ledger, "snapshot", "PASS",\n            manifest_sha256=manifest_sha, source_fingerprint=source_fp, file_count=len(source)\n        )\n        return {"session": session, "frozen": frozen, "source": source, "source_fingerprint": source_fp}\n    except Exception:\n        shutil.rmtree(session, ignore_errors=True)\n        raise\n\ndef _clone_tree(src: Path, dst: Path) -> None:\n    shutil.copytree(src, dst, symlinks=True)\n    _assert_no_symlinks(dst)\n\ndef _inventory(base: Path) -> dict[str, dict[str, Any]]:\n    out: dict[str, dict[str, Any]] = {}\n    for p in sorted(base.rglob("*")):\n        rel = p.relative_to(base).as_posix()\n        if p.is_symlink():\n            out[rel] = {"type": "symlink", "target": os.readlink(p)}\n        elif p.is_file():\n            st = p.stat()\n            out[rel] = {"type": "file", "sha256": _sha_file(p), "size": st.st_size, "mode": st.st_mode & 0o777}\n    return out\n\ndef _changed(before: dict[str, Any], after: dict[str, Any]) -> list[str]:\n    return sorted(k for k in set(before) | set(after) if before.get(k) != after.get(k))\n\ndef _assert_no_symlinks(box: Path) -> None:\n    bad = [p.relative_to(box).as_posix() for p in box.rglob("*") if p.is_symlink()]\n    if bad:\n        raise RuntimeError("sandbox_symlink_rejected:" + ",".join(bad[:20]))\n\ndef _normalize_allowed(values: Any) -> list[str]:\n    if not isinstance(values, list) or not values or len(values) > MAX_ALLOWED_PATHS:\n        raise RuntimeError("allowed_paths_required_or_excessive")\n    out = sorted(_safe_rel(str(x)) for x in values)\n    if len(set(out)) != len(out):\n        raise RuntimeError("allowed_paths_duplicates_rejected")\n    return out\n\ndef _protected(rel: str) -> bool:\n    return any(rel.startswith(prefix) for prefix in PROTECTED_PREFIXES)\n\ndef _load_bound_json_receipt(root: Path, spec: Any, prefixes: tuple[str, ...]) -> dict[str, Any]:\n    if not isinstance(spec, dict):\n        raise RuntimeError("bound_receipt_required")\n    rel = _safe_rel(str(spec.get("path") or ""))\n    if not any(rel.startswith(prefix) for prefix in prefixes):\n        raise RuntimeError("receipt_path_not_trusted:" + rel)\n    expected = str(spec.get("sha256") or "").lower()\n    if len(expected) != 64 or any(c not in "0123456789abcdef" for c in expected):\n        raise RuntimeError("receipt_sha256_required")\n    p = root / rel\n    if p.is_symlink() or not p.is_file():\n        raise RuntimeError("trusted_receipt_unavailable:" + rel)\n    raw = p.read_bytes()\n    if _sha(raw) != expected:\n        raise RuntimeError("trusted_receipt_hash_mismatch:" + rel)\n    obj = json.loads(raw.decode("utf-8"))\n    if not isinstance(obj, dict):\n        raise RuntimeError("trusted_receipt_json_object_required")\n    return obj\n\ndef _authenticate_evidence(root: Path, evidence: Any) -> list[dict[str, Any]]:\n    if not isinstance(evidence, list) or not evidence:\n        raise RuntimeError("structured_failure_evidence_required")\n    if len(json.dumps(evidence, sort_keys=True).encode()) > MAX_EVIDENCE_BYTES:\n        raise RuntimeError("failure_evidence_too_large")\n    out: list[dict[str, Any]] = []\n    for row in evidence:\n        if not isinstance(row, dict):\n            raise RuntimeError("failure_evidence_invalid")\n        for k in ("source", "observation", "reproduction"):\n            if not str(row.get(k) or "").strip():\n                raise RuntimeError("failure_evidence_incomplete")\n        receipt = _load_bound_json_receipt(root, row.get("receipt"), TRUSTED_EVIDENCE_PREFIXES)\n        out.append({**row, "verified_receipt_schema": receipt.get("schema"), "verified_receipt": True})\n    return out\n\ndef _authorization(root: Path, request: dict[str, Any], job_id: str, allowed: list[str]) -> dict[str, Any] | None:\n    protected = sorted(x for x in allowed if _protected(x))\n    deletions = sorted(_safe_rel(str(x)) for x in (request.get("owner_authorized_deletions") or []))\n    if not protected and not deletions:\n        return None\n    obj = _load_bound_json_receipt(root, request.get("owner_authorization_receipt"), (AUTHORIZATION_PREFIX,))\n    if str(obj.get("job_id") or "") != job_id:\n        raise RuntimeError("owner_authorization_job_mismatch")\n    approved_paths = sorted(_safe_rel(str(x)) for x in (obj.get("protected_paths") or []))\n    approved_deletions = sorted(_safe_rel(str(x)) for x in (obj.get("deletions") or []))\n    if protected != approved_paths:\n        raise RuntimeError("owner_authorization_protected_scope_mismatch")\n    if deletions != approved_deletions:\n        raise RuntimeError("owner_authorization_deletion_scope_mismatch")\n    if not str(obj.get("authorized_by") or "").strip():\n        raise RuntimeError("owner_authorization_identity_missing")\n    return obj\n\ndef _parse_plan(stdout: str) -> dict[str, Any]:\n    raw = stdout.encode("utf-8")\n    if len(raw) > MAX_PLAN_BYTES:\n        raise RuntimeError("opencode_plan_too_large")\n    lines = [x.strip() for x in stdout.splitlines() if x.strip()]\n    if len(lines) != 1:\n        raise RuntimeError("opencode_plan_must_be_single_json_line")\n    try:\n        obj = json.loads(lines[0])\n    except Exception as exc:\n        raise RuntimeError("opencode_plan_json_required") from exc\n    if set(obj) != {"file_scope", "plan"} or not isinstance(obj.get("file_scope"), list) or not isinstance(obj.get("plan"), str):\n        raise RuntimeError("opencode_plan_contract_invalid")\n    obj["file_scope"] = sorted(_safe_rel(str(x)) for x in obj["file_scope"])\n    if not obj["file_scope"] or len(obj["file_scope"]) > MAX_ALLOWED_PATHS:\n        raise RuntimeError("opencode_plan_scope_invalid")\n    return obj\n\ndef _validate_inspect_request(request: dict[str, Any]) -> tuple[str, str]:\n    if not isinstance(request, dict):\n        raise TypeError("engineering_worker_request_must_be_dict")\n    return _safe_job_id(request.get("job_id")), _bounded_text(request.get("objective"), MAX_OBJECTIVE_BYTES, "objective")\n\ndef _validate_build_request(request: dict[str, Any]) -> dict[str, Any]:\n    job_id, objective = _validate_inspect_request(request)\n    evidence = request.get("failure_evidence") or []\n    interference = request.get("organism_interference") or []\n    if request.get("healthy") is True:\n        if evidence or interference:\n            return {"decision": "REJECT_CONTRADICTORY_HEALTH_STATE", "ok_to_build": False, "job_id": job_id, "objective": objective}\n        return {"decision": "CHILL_AND_MOVE_ON", "ok_to_build": False, "job_id": job_id, "objective": objective}\n    if request.get("would_create_overlap") or request.get("canonical_owner_conflict"):\n        return {"decision": "REJECT_OVERLAP", "ok_to_build": False, "job_id": job_id, "objective": objective}\n    if request.get("would_regress_working_behavior"):\n        return {"decision": "REJECT_REGRESSION", "ok_to_build": False, "job_id": job_id, "objective": objective}\n    return {"decision": "BUILD_REPAIR_OR_REPLACEMENT", "ok_to_build": True, "job_id": job_id, "objective": objective}\n\ndef _test_argv(argv: Any) -> list[str]:\n    if not isinstance(argv, list) or not argv or not all(isinstance(x, str) and x for x in argv):\n        raise RuntimeError("invalid_test_argv")\n    exe = Path(argv[0])\n    if not exe.is_absolute() or exe not in {PINNED["python3"], PINNED["git"]}:\n        raise RuntimeError("test_executable_not_allowlisted")\n    for arg in argv[1:]:\n        if arg.startswith("/") or ".." in Path(arg).parts:\n            raise RuntimeError("unsafe_test_argument:" + arg)\n    if exe == PINNED["python3"] and "-c" in argv:\n        raise RuntimeError("python_inline_code_not_allowed")\n    return list(argv)\n\ndef _syntax_check_python(box: Path, rel: str) -> dict[str, Any]:\n    code = "import pathlib,sys; p=pathlib.Path(sys.argv[1]); compile(p.read_text(encoding=\'utf-8\'), str(p), \'exec\')"\n    return _run([_binary("python3"), "-c", code, rel], box, 120, network=False)\n\ndef _run_tests(\n    box: Path,\n    changed: list[str],\n    request: dict[str, Any],\n    job_dir: Path,\n    ledger: list[dict[str, Any]],\n    label: str,\n) -> list[dict[str, Any]]:\n    commands = [request.get("verification_argv")] + list(request.get("test_matrix") or [])\n    if len(commands) > MAX_TEST_COMMANDS:\n        raise RuntimeError("test_matrix_too_large")\n    tests: list[dict[str, Any]] = []\n    for rel in changed:\n        p = box / rel\n        if rel.endswith(".py") and p.is_file():\n            r = _syntax_check_python(box, rel)\n            tests.append({"name": "syntax:" + rel, "ok": r["returncode"] == 0, "returncode": r["returncode"], "stderr": r["stderr"][-2000:]})\n    for i, raw in enumerate(commands):\n        argv = _test_argv(raw)\n        timeout = min(int(request.get("test_timeout_seconds") or STAGE_TIMEOUTS["test"]), STAGE_TIMEOUTS["test"])\n        r = _run(argv, box, timeout, network=False)\n        tests.append({\n            "name": "verification" if i == 0 else f"matrix_{i}",\n            "ok": r["returncode"] == 0,\n            "returncode": r["returncode"],\n            "timed_out": r["timed_out"],\n            "output_overflow": r["output_overflow"],\n            "stdout": r["stdout"][-4000:],\n            "stderr": r["stderr"][-2000:],\n        })\n    _append_receipt(job_dir, ledger, label, "PASS" if tests and all(t["ok"] for t in tests) else "FAIL", tests=tests)\n    return tests\n\ndef _review_packet(\n    frozen: Path,\n    candidate: Path,\n    changed: list[str],\n    objective: str,\n    evidence: list[dict[str, Any]],\n) -> bytes:\n    parts: list[str] = [\n        "EIRA ENGINEERING REVIEW PACKET V1",\n        "OBJECTIVE:\\n" + objective,\n        "EVIDENCE:\\n" + json.dumps(evidence, sort_keys=True),\n        "CHANGED_PATHS:\\n" + json.dumps(changed),\n    ]\n    total = sum(len(x.encode("utf-8")) for x in parts)\n    for rel in changed:\n        a, b = frozen / rel, candidate / rel\n        before = a.read_bytes() if a.is_file() else b""\n        after = b.read_bytes() if b.is_file() else b""\n        try:\n            at = before.decode("utf-8").splitlines(keepends=True)\n            bt = after.decode("utf-8").splitlines(keepends=True)\n            section = "".join(difflib.unified_diff(at, bt, fromfile="a/" + rel, tofile="b/" + rel, n=3))\n        except UnicodeDecodeError:\n            section = (\n                f"BINARY {rel} before_sha256={_sha(before)} before_size={len(before)} "\n                f"after_sha256={_sha(after)} after_size={len(after)}\\n"\n            )\n        total += len(section.encode("utf-8"))\n        if total > MAX_REVIEW_BYTES:\n            raise RuntimeError("review_packet_too_large")\n        parts.append(section)\n    packet = "\\n".join(parts).encode("utf-8")\n    if len(packet) > MAX_REVIEW_BYTES:\n        raise RuntimeError("review_packet_too_large")\n    return packet\n\ndef _parse_review(stdout: str) -> bool:\n    lines = [x.strip() for x in stdout.splitlines() if x.strip()]\n    return bool(lines) and lines[-1] == "REVIEW=PASS"\n\ndef _bundle(\n    box: Path,\n    baseline: dict[str, Any],\n    changed: list[str],\n    source_fp: str,\n    allowed_deletions: set[str],\n) -> dict[str, Any]:\n    _assert_no_symlinks(box)\n    final = _inventory(box)\n    if _changed(baseline, final) != changed:\n        raise RuntimeError("candidate_changed_before_seal")\n    files: dict[str, Any] = {}\n    total = 0\n    for rel in changed:\n        meta, before, p = final.get(rel), baseline.get(rel), box / rel\n        if meta is None:\n            if rel not in allowed_deletions:\n                raise RuntimeError("deletion_requires_owner_authorization:" + rel)\n            files[rel] = {"operation": "delete", "before": before, "after": None}\n            continue\n        if meta.get("type") != "file":\n            raise RuntimeError("non_file_candidate_rejected:" + rel)\n        data = p.read_bytes()\n        st = p.stat()\n        actual = {"type": "file", "sha256": _sha(data), "size": len(data), "mode": st.st_mode & 0o777}\n        if actual != meta:\n            raise RuntimeError("candidate_raced_during_seal:" + rel)\n        total += len(data)\n        if total > MAX_CANDIDATE_BYTES:\n            raise RuntimeError("candidate_byte_limit_exceeded")\n        files[rel] = {"operation": "write", "before": before, "after": actual, "content_hex": data.hex()}\n    env = {"schema": "eira2_engineering_candidate_bundle_v3", "source_fingerprint": source_fp, "files": files}\n    env["bundle_sha256"] = _sha(json.dumps(env, sort_keys=True, separators=(",", ":")).encode())\n    return env\n\ndef inspect(request: dict[str, Any]) -> dict[str, Any]:\n    job_id, objective = _validate_inspect_request(request)\n    ledger: list[dict[str, Any]] = []\n    job_dir = _job_dir(job_id)\n    old = _completed_receipt(job_dir)\n    if old:\n        return old\n    session: Path | None = None\n    try:\n        with _worker_lock():\n            tools = _tool_set()\n            model_digest = _ollama_digest()\n            root = Path(request.get("root") or os.environ.get("EIRA_ROOT") or Path.cwd()).resolve()\n            ws = _workspace(root, job_dir, ledger)\n            session = ws["session"]\n            box = session / "opencode"\n            _clone_tree(ws["frozen"], box)\n            baseline = _inventory(box)\n            prompt = (\n                "Inspect this frozen canonical EIRA package snapshot. Do not edit. "\n                "Use snapshot evidence only; do not invent LIVE runtime truth. "\n                "Identify exact defect candidates, canonical owner, reproduction path, and bounded file scope. "\n                "If insufficient evidence, say so. OBJECTIVE: " + objective\n            )\n            _append_receipt(job_dir, ledger, "opencode_inspect", "START")\n            r = _run([_binary("opencode"), "run", "--model", OPENCODE_MODEL, prompt], box, STAGE_TIMEOUTS["inspect"], network=True)\n            mutations = _changed(baseline, _inventory(box))\n            _assert_tool_set(tools)\n            if _ollama_digest() != model_digest:\n                raise RuntimeError("ollama_model_identity_changed_during_job")\n            ok = r["returncode"] == 0 and not mutations\n            _append_receipt(job_dir, ledger, "opencode_inspect", "PASS" if ok else "FAIL", mutations=mutations, returncode=r["returncode"])\n            result = {\n                "schema": SCHEMA, "mode": "inspect", "job_id": job_id, "ok": ok,\n                "source_fingerprint": ws["source_fingerprint"], "opencode": r,\n                "ledger": ledger, "tool_receipts": tools, "model_digest": model_digest,\n                "live_mutated": False,\n            }\n            _seal_completed(job_dir, result)\n            return result\n    except OSError as exc:\n        if exc.errno == errno.EIO:\n            _append_receipt(job_dir, ledger, "storage_integrity", "STOP", error=str(exc))\n            return {"schema": SCHEMA, "mode": "inspect", "job_id": job_id, "ok": False, "storage_integrity_stop": True, "error": "EIO:" + str(exc), "ledger": ledger, "live_mutated": False}\n        raise\n    finally:\n        if session:\n            shutil.rmtree(session, ignore_errors=True)\n\ndef build(request: dict[str, Any]) -> dict[str, Any]:\n    issue = _validate_build_request(request)\n    job_id, objective = issue["job_id"], issue["objective"]\n    if issue["ok_to_build"] is not True:\n        return {"schema": SCHEMA, "mode": "build", "job_id": job_id, "ok": False, "organism_issue_gate": issue, "live_mutated": False}\n\n    ledger: list[dict[str, Any]] = []\n    job_dir = _job_dir(job_id)\n    old = _completed_receipt(job_dir)\n    if old:\n        return old\n    session: Path | None = None\n    try:\n        with _worker_lock():\n            tools = _tool_set()\n            model_digest = _ollama_digest()\n            root = Path(request.get("root") or os.environ.get("EIRA_ROOT") or Path.cwd()).resolve()\n            evidence = _authenticate_evidence(root, request.get("failure_evidence"))\n            allowed = _normalize_allowed(request.get("allowed_paths"))\n            auth = _authorization(root, request, job_id, allowed)\n            delete_ok = set(_safe_rel(str(x)) for x in (request.get("owner_authorized_deletions") or []))\n            ws = _workspace(root, job_dir, ledger)\n            session = ws["session"]\n            baseline = _inventory(ws["frozen"])\n\n            plan_box = session / "opencode"\n            _clone_tree(ws["frozen"], plan_box)\n            _append_receipt(job_dir, ledger, "opencode_plan", "START")\n            prompt = (\n                "Return exactly one JSON line with exactly keys file_scope(list) and plan(string). "\n                "Do not edit. Scope only exact relative files justified by evidence. "\n                "No absolute paths, duplicate authorities, or cosmetic churn. OBJECTIVE: "\n                + objective + "\\nEVIDENCE:\\n" + json.dumps(evidence, sort_keys=True)\n            )\n            plan_run = _run([_binary("opencode"), "run", "--model", OPENCODE_MODEL, prompt], plan_box, STAGE_TIMEOUTS["plan"], network=True)\n            if plan_run["returncode"] != 0 or _changed(baseline, _inventory(plan_box)):\n                raise RuntimeError("opencode_plan_failed_or_mutated")\n            plan = _parse_plan(plan_run["stdout"])\n            if plan["file_scope"] != allowed:\n                raise RuntimeError("opencode_scope_must_equal_authorized_scope")\n            _append_receipt(job_dir, ledger, "opencode_plan", "PASS", file_scope=allowed)\n\n            successful_box: Path | None = None\n            changed: list[str] = []\n            last_failure = ""\n            for attempt in range(1, MAX_REPAIR_ATTEMPTS + 1):\n                attempt_box = session / f"aider_{attempt}"\n                _clone_tree(ws["frozen"], attempt_box)\n                aider_prompt = (\n                    "Implement only the authorized repair in these exact files:\\n" + "\\n".join(allowed)\n                    + "\\nNo other paths. No backups/versioned alternates. Stay inside /workspace. Preserve unrelated behavior."\n                    + "\\nOBJECTIVE:\\n" + objective + "\\nPLAN:\\n" + plan["plan"]\n                )\n                if last_failure:\n                    aider_prompt += "\\nPREVIOUS QUALIFICATION FAILURE:\\n" + last_failure[-8000:]\n                _append_receipt(job_dir, ledger, f"aider_{attempt}", "START")\n                argv = [\n                    _binary("aider"), "--yes-always", "--no-auto-commits", "--no-dirty-commits",\n                    "--model", AIDER_MODEL, "--message", aider_prompt\n                ] + allowed\n                ad = _run(argv, attempt_box, STAGE_TIMEOUTS["aider"], network=True)\n                _assert_no_symlinks(attempt_box)\n                attempt_changed = _changed(baseline, _inventory(attempt_box))\n                if len(attempt_changed) > MAX_CHANGED_FILES or any(rel not in allowed for rel in attempt_changed):\n                    raise RuntimeError("candidate_scope_violation")\n                if ad["returncode"] != 0:\n                    _append_receipt(job_dir, ledger, f"aider_{attempt}", "FAIL", returncode=ad["returncode"], changed=attempt_changed)\n                    last_failure = "aider_returncode=" + str(ad["returncode"]) + "\\n" + ad["stderr"]\n                    continue\n                if not attempt_changed:\n                    _append_receipt(job_dir, ledger, f"aider_{attempt}", "FAIL", reason="no_material_change")\n                    last_failure = "no_material_change"\n                    continue\n                pre = _inventory(attempt_box)\n                tests = _run_tests(attempt_box, attempt_changed, request, job_dir, ledger, f"qualification_{attempt}")\n                if _inventory(attempt_box) != pre:\n                    raise RuntimeError("tests_mutated_candidate")\n                if tests and all(t["ok"] for t in tests):\n                    successful_box, changed = attempt_box, attempt_changed\n                    _append_receipt(job_dir, ledger, f"aider_{attempt}", "PASS", changed=changed)\n                    break\n                last_failure = json.dumps(tests, sort_keys=True)\n            if successful_box is None:\n                raise RuntimeError("repair_attempts_exhausted")\n\n            _assert_no_symlinks(successful_box)\n            review_box = session / "review"\n            _clone_tree(successful_box, review_box)\n            packet = _review_packet(ws["frozen"], successful_box, changed, objective, evidence)\n            review_file = review_box / "EIRA_REVIEW_CANDIDATE.packet"\n            review_file.write_bytes(packet)\n            review_baseline = _inventory(review_box)\n            review_prompt = (\n                "Adversarially review the complete candidate using EIRA_REVIEW_CANDIDATE.packet. "\n                "Do not edit. Reject scope drift, unproven fixes, regressions, duplicate ownership, "\n                "protected-authority weakening, or insufficient verification. "\n                "Final nonblank line exactly REVIEW=PASS or REVIEW=FAIL."\n            )\n            _append_receipt(job_dir, ledger, "opencode_review", "START")\n            rev = _run([_binary("opencode"), "run", "--model", OPENCODE_MODEL, review_prompt], review_box, STAGE_TIMEOUTS["review"], network=True)\n            if _inventory(review_box) != review_baseline:\n                raise RuntimeError("opencode_review_mutated_review_workspace")\n            if rev["returncode"] != 0 or not _parse_review(rev["stdout"]):\n                raise RuntimeError("adversarial_review_failed")\n            _append_receipt(job_dir, ledger, "opencode_review", "PASS", review_packet_sha256=_sha(packet))\n\n            pre_final = _inventory(successful_box)\n            final_tests = _run_tests(successful_box, changed, request, job_dir, ledger, "final_qualification")\n            if not final_tests or any(t["ok"] is not True for t in final_tests):\n                raise RuntimeError("final_qualification_failed")\n            if _inventory(successful_box) != pre_final:\n                raise RuntimeError("final_tests_mutated_candidate")\n            bundle = _bundle(successful_box, baseline, changed, ws["source_fingerprint"], delete_ok)\n\n            _assert_tool_set(tools)\n            if _ollama_digest() != model_digest:\n                raise RuntimeError("ollama_model_identity_changed_during_job")\n            _append_receipt(job_dir, ledger, "candidate_seal", "PASS", bundle_sha256=bundle["bundle_sha256"], changed=changed)\n\n            result = {\n                "schema": SCHEMA, "mode": "build", "job_id": job_id, "ok": True,\n                "source_fingerprint": ws["source_fingerprint"], "changed_paths": changed,\n                "candidate_bundle": bundle, "review_passed": True, "tests": final_tests,\n                "ledger": ledger, "tool_receipts": tools, "model_digest": model_digest,\n                "authorization_receipt_verified": bool(auth),\n                "live_mutated": False, "requires_watcher_builder_deployment": True,\n                "deployment_contract": {\n                    "schema": "eira2_engineering_deployment_contract_v1",\n                    "required": [\n                        "Watcher re-authenticates evidence and any owner authorization",\n                        "Watcher rechecks LIVE source fingerprint and every before hash",\n                        "canonical Builder applies entire bundle transactionally",\n                        "rollback on any write or verification failure",\n                        "package identity reseal",\n                        "Superprobe post-deploy verification",\n                        "durable LIVE receipt with final hashes and bundle_sha256",\n                    ],\n                },\n            }\n            _seal_completed(job_dir, result)\n            return result\n    except OSError as exc:\n        if exc.errno == errno.EIO:\n            _append_receipt(job_dir, ledger, "storage_integrity", "STOP", error=str(exc))\n            return {"schema": SCHEMA, "mode": "build", "job_id": job_id, "ok": False, "storage_integrity_stop": True, "error": "EIO:" + str(exc), "ledger": ledger, "live_mutated": False}\n        raise\n    except Exception as exc:\n        _append_receipt(job_dir, ledger, "failure", "FAIL", error=type(exc).__name__ + ":" + str(exc))\n        return {"schema": SCHEMA, "mode": "build", "job_id": job_id, "ok": False, "error": type(exc).__name__ + ":" + str(exc), "ledger": ledger, "live_mutated": False}\n    finally:\n        if session:\n            shutil.rmtree(session, ignore_errors=True)\n\ndef self_test() -> dict[str, Any]:\n    checks: dict[str, bool] = {}\n    for bad in ("", ".", "./", "../x", "/tmp/x", "a/../b"):\n        try:\n            _safe_rel(bad)\n            checks["reject:" + bad] = False\n        except RuntimeError:\n            checks["reject:" + bad] = True\n    checks["review_exact"] = _parse_review("x\\nREVIEW=PASS\\n")\n    checks["review_embedded_rejected"] = not _parse_review("foo REVIEW=PASS bar")\n    checks["models_role_pinned"] = OPENCODE_MODEL != AIDER_MODEL\n    checks["direct_live_write_false"] = True\n    checks["state_root_outside_live"] = "/EIRA/LIVE/" not in str(STATE_ROOT)\n    return {"schema": SCHEMA, "ok": all(checks.values()), "checks": checks}\n\ndef status(root: str | Path | None = None) -> dict[str, Any]:\n    r = Path(root or os.environ.get("EIRA_ROOT") or Path.cwd()).resolve()\n    tools = {}\n    for k in PINNED:\n        try:\n            tools[k] = _tool_receipt(k)\n        except Exception as exc:\n            tools[k] = {"ok": False, "error": str(exc)}\n    try:\n        model_digest = _ollama_digest()\n    except Exception as exc:\n        model_digest = "UNAVAILABLE:" + str(exc)\n    return {\n        "schema": SCHEMA, "extension_id": EXTENSION_ID, "root": str(r),\n        "direct_live_write": False,\n        "filesystem_containment": "bubblewrap required for OpenCode/Aider/tests",\n        "test_network_containment": "network namespace isolated",\n        "model_network": "host loopback required only for local Ollama",\n        "deployment_authority": "Watcher -> canonical Builder",\n        "durable_job_state": str(STATE_ROOT),\n        "tools": tools, "model_digest": model_digest,\n    }\n\ndef ask(request: dict[str, Any] | str) -> dict[str, Any]:\n    if isinstance(request, str):\n        request = json.loads(request)\n    if not isinstance(request, dict):\n        raise TypeError("engineering_worker_request_must_be_dict")\n    mode = str(request.get("mode") or "status")\n    if mode == "status":\n        return status(request.get("root"))\n    if mode == "inspect":\n        return inspect(request)\n    if mode in {"build", "repair", "self_repair"}:\n        return build(request)\n    if mode == "continuous_cycle":\n        return build(request) if request.get("failure_evidence") else inspect(request)\n    if mode == "self_test":\n        return self_test()\n    raise RuntimeError("unsupported_engineering_worker_mode:" + mode)\n\ndef capabilities() -> dict[str, Any]:\n    return {\n        "extension_id": EXTENSION_ID, "schema": SCHEMA,\n        "modes": ["status", "inspect", "build", "repair", "self_repair", "continuous_cycle", "self_test"],\n        "discipline": "repair authenticated verified defects only; CHILL when healthy; no LIVE writes",\n        "queue_owner": "external transport/supervisor",\n        "live_deployment": "Watcher -> canonical Builder only",\n    }\n'

if __name__ == "__main__":
    compile(NEW, TARGET, "exec")
    print("EIRA2_ENGINEERING_WORKER_V6_CANONICAL_HARDENED=PASS")
